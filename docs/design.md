# Design: Probabilistic Demand Forecasting & Inventory Policy Simulator

Status: approved with amendments, 2026-09-20. This is the source of truth for Phases 2-14.
Where a later phase needs to change something here, edit this file and add a line to the decision log (section 12).

Repo name: `probabilistic-demand-forecasting-simulator` (the folder was originally called "Supply Chain Churn Prediction", a misnomer: this is demand forecasting, not churn).

## 1. What we will and will not claim

- Every number in the README and on the resume must come from a logged run or simulation output. Nothing is quoted before it is measured.
- The headline result is an **inventory-vs-service-level trade-off curve at matched service levels** (section 9), not a single cost number.
- If the quantile policy does not win somewhere (e.g. fast movers, or at 0.99), that is reported as is.
- Simulation results describe *historical replay under stated assumptions*. They are not a claim about real savings.

## 2. Dataset and subset

**M5 Forecasting (Walmart), from Kaggle.** Download manually (account + accepting competition rules). Check the licence on the competition Data page. Raw files live in `data/raw/` and are git-ignored.

| File | Contents (from the competition docs; verified on load in Phase 2) |
|---|---|
| `sales_train_evaluation.csv` | wide format, one row per item-store series; id columns (`id`, `item_id`, `dept_id`, `cat_id`, `store_id`, `state_id`; `id` is a composite key such as `FOODS_1_001_CA_1_evaluation`) and daily unit sales `d_1..d_1941` |
| `calendar.csv` | date, weekday, month, year, `wm_yr_wk` (price join key), `event_name/type_1/2`, `snap_CA/TX/WI` |
| `sell_prices.csv` | weekly `sell_price` per store and item, keyed by `wm_yr_wk` |

**Sales data spans 2011-01-29 to 2016-05-22** (`d_1..d_1941`; 22 May 2016 is a Sunday). `calendar.csv` runs 28 days further, to 2016-06-19 (1,969 days, the M5 evaluation horizon); those days have no sales and are never used. Verified on load in Phase 2.

**Subset:** state CA, stores CA_1..CA_3, 100 items sampled with a fixed seed, stratified by department and by sales-velocity tercile so slow and intermittent items are represented. That is 300 series (509,871 daily rows after dropping 72,429 pre-launch rows; 582,300 before). The eligible pool is items on sale in **all three** CA stores for at least 2 years (730 days) before the tuning cutoff (the first `sell_price` week starts on or before `2015-08-30 - 730 days`). Measured in Phase 2/3: 2,215 of 3,049 items (2,221 under a 31 Aug cutoff; see the decision log). Everything else is dropped and the count is documented. Scale up only if runtime allows.

**Velocity segments (high / mid / low):** terciles of each series' mean daily sales since its first on-sale week, computed **once, from data up to the tuning cutoff (`test_start - 12 weeks`, section 5) only**. Items are sampled stratified on their mean velocity tercile across the three stores; each series carries its own segment label. The same labels drive the stratified sampling, the pooling of sigma and conformal offsets (section 5), and the result breakdowns (section 9). They are never recomputed with later data, because a segment defined using test-period sales would leak the outcome into how results are grouped and calibrated.

**Cleaning rules known in advance:** rows before an item's first `sell_price` are "not yet on sale", not zero demand, and are dropped. The rule is applied as written to every series, including `FOODS_2_101_CA_2`, whose first sale comes 1,578 days after its first price (kept; its long leading zero run is not special-cased and is noted as a data oddity). Days when the store is closed (Christmas is the known case) are near-zero, not exactly zero: in Phase 2, CA_1..3 total sales across all 3,049 items were 5-8 units on each Dec 25 of 2011-2015, against thousands on a normal day. A closed day is therefore defined by a **near-zero threshold on total store sales** (threshold and the list of closed dates confirmed in Phase 3 EDA), not `== 0`. Such days are flagged, and the calendar rule derived from them is applied to 2015 (section 5).

**Limitations (must appear in the README):**
1. No inventory levels, lead times, or costs in the data. All are simulated assumptions.
2. Sales are not demand: history is censored when stock ran out. We forecast sales as a proxy for demand, and the data is **not filtered** for it. Measured in Phase 3 (training data only): 80 of 100 high-velocity series have a zero run of 14+ open days, about 11% of high-velocity days (15% mid, 34% low) sit inside such runs, and these runs are strongly simultaneous across an item's three stores (see section 12), so part of it is item-level (delisting, supply, seasonal items) rather than store stock-outs. The simulator replays zero demand inside such runs, which flatters every policy on inventory.
3. No promotion column. We derive `price vs. usual` as a proxy. Model outputs and SHAP text say "price is X% below usual", never "promotion".
4. Anonymised items, one retailer, 2011-2016.

## 3. Target, horizon, quantiles

**Target:** cumulative demand over the next P days from forecast origin t, using only information available at the end of day t:

`Y(s, t, P) = sales[s, t+1] + ... + sales[s, t+P]`

Why cumulative: daily SKU-store demand is mostly 0-2 units, so a daily interval is meaningless, and quantiles of daily forecasts cannot be summed to get the quantile of lead-time demand (the sum of P90s equals the P90 of the sum only under perfect dependence, so it over-orders). Training directly on the P-day sum gives the right quantity, with day-to-day dependence handled implicitly and no distributional assumption. Cost: one model per horizon, and overlapping windows make consecutive training rows correlated (evaluation uses weekly origins for that reason). Rejected alternatives: Monte Carlo from daily distributions (needs a copula), parametric negative binomial (extra assumption; possible stretch).

**Horizons:** review period R = 7 days, lead time L in {3, 7}, protection interval P = R + L in {10, 14}, plus P = 7 for the "7-day forecast" display. So **P in {7, 10, 14}**.

**Quantile set:** {0.10, 0.50, 0.80, 0.90, 0.95, 0.99}.
- P10 / P50 / P90 give the displayed forecast and the 80% interval.
- {0.80, 0.90, 0.95, 0.99} are the service-level quantiles used by the policy.
- Sorted (monotone rearrangement) after prediction to fix quantile crossing.
- 0.99 is a tail estimate on limited data. It is evaluated explicitly (one-sided coverage), not assumed to work.

**Global model:** one model per horizon across all 300 series, with item/store/department as features.

**Target normalization (experiment, decided on validation):** train on `Y / scale(s, t)` with scale = trailing mean daily sales (past data only, floored) times P, then multiply predictions back. Valid because quantiles commute with multiplication by a positive constant. It makes training loss equal to scaled pinball (each series weighted equally, not dominated by fast movers). Risks: floor choice for near-dead series, lumpiness at small scales. A/B against the raw target on the tuning folds using the scaled metrics of section 6; the choice is frozen before the test window is touched. Phase 6 reports raw and normalised side by side and makes no final choice; the final decision is taken in Phase 7 on scaled pinball loss (section 12).

## 4. Features (as of origin t; details and leakage notes in Phase 4)

- **History (all computed from data <= t):** recent daily sales lags, rolling mean/std over 7/14/28/91 days, fraction of zero days, days since last sale, **longest zero run in the last 91 days (capped at 56)**, sum of the last P days, sum of the same window 364 days earlier, current price, item/store/dept/cat ids.
- **Future-window covariate aggregates (window t+1..t+P):** number of SNAP days (state-specific), number of event days (by type where useful), number of weekend days, number of store-closed days (a calendar rule, see section 5), **planned price relative to trailing median** (mean and min window price divided by the trailing median price, the median computed from prices <= t).
- The exact list, windows and a leakage note for every feature are in `docs/features.md` (Phase 4); `ml/features.py` builds it and `ml/test_features.py` checks the leakage rules by perturbing the data after an origin.
- **Known-prices assumption (stated, not hidden):** future prices, events and SNAP days are treated as known at order time. Events and SNAP schedules genuinely are. Prices are an assumption: M5 gives us realised prices, and we treat them as if they were the plan. That flatters ML policies relative to the naive rule, so we run an ablation without future-price features and report the difference.
- Future sales are never a feature. Predictive effects of price are associations, not causal estimates (why "what-if" price sliders are out of scope).

## 5. Splits and leakage rules

Timeline: **test = last 26 weeks.** Concretely, the **first review date is Sunday 2015-11-22** (`test_start`), and **test demand days run Monday 2015-11-23 to Sunday 2016-05-22** (182 days, 26 whole weeks). **Reviews are on Sundays for every series** (the fixed review weekday, section 9); an order placed after close on a Sunday covers demand from the Monday. Model selection and tuning use rolling-origin folds on data up to `test_start - 12 weeks` = **Sunday 2015-08-30** (the tuning cutoff) only.

For a model version with cutoff date `c` (retrained every K = 8 weeks on an expanding window):
- **Fit set:** origins `s` with `s + P <= c - 84 days`. The target window must have fully ended before the cutoff (embargo), otherwise labels leak across the train/test boundary.
- **Calibration window:** the 12 weeks before `c`. Out-of-sample residuals from here give the point policy's sigma and the optional conformal offsets. Same window, same pooling (below) for both policies.
- **Use:** review dates in `[c, c + K weeks)`.
- **Model staleness is 12 to 20 weeks, not 12.** The fit set ends at `c - 84 days` and a version is used for review dates up to `c + 8 weeks`, so the newest data the model has seen is 12 weeks old at the start of its use period and 20 weeks old at the end. Simplification (`ponytail`): the model is not refit on the calibration window. Upgrade path: refit after calibrating (staleness drops to 0-8 weeks), keeping the calibration offsets, at the price of slightly optimistic residuals.
- **Calibration is pooled by velocity segment, never estimated per series alone, for both policies.** A 12-week window gives about 12 weekly origins per series (about 84 daily origins, but overlapping and strongly dependent). That is too few for a reliable per-series sigma, and it cannot support a 0.99 conformal offset at all (a 0.99 quantile needs on the order of 100 scores; the maximum of 12 is not that). Rule: residuals and conformal scores are divided by the series' scale (P x trailing mean daily sales, floored; the same scale as the normalization in section 3), pooled within the velocity segment, and the pooled statistic is multiplied back by the series' own scale: `sigma_s = sigma_segment * scale_s`, `offset_s,alpha = offset_segment,alpha * scale_s`. This assumes a roughly constant coefficient of variation within a segment; Phase 9 checks it with a residual-vs-scale diagnostic (fallback: scale by the square root of the scale, which is Poisson-like). Shrinking each series toward its segment value is the alternative if pooling is too coarse. Same window, same segments, same rule for the point policy's sigma and the quantile policy's offsets, so neither gets better-calibrated inputs than the other.

General rules: never shuffle; nothing (scalers, price medians, scale factors, item sampling thresholds) is fitted on data after the origin; all history features are computed as-of t; test data is never used for tuning; the test-window forecasts are computed once and reused across policies (forecasts depend only on historical sales, not on simulated stock).

**Origins:** an origin is evaluated only if its whole target window lies inside the data, and the simulator needs the full protection interval too. The longest horizon is P = 14, so the **last usable review date is Sunday 2016-05-08, 14 days before 2016-05-22**. That gives **25 Sunday review dates** (2015-11-22 to 2016-05-08). The same review dates are used for every P (including P = 7 and 10, which could go a little later) so results are comparable across horizons and policies; the last two demand weeks are only ever the tail of a protection interval.

**Test window regime (read before interpreting any result).** The test demand days (23 Nov 2015 to 22 May 2016) include the Thanksgiving-Christmas-New Year peak, **closed-store days** such as Christmas Day (near-zero sales; threshold confirmed in Phase 3), the Super Bowl and Easter.
- Results mix a holiday-peak regime with a normal one. We therefore report two sub-periods on demand days, with boundaries fixed now: **holiday peak = 23 Nov 2015 to 3 Jan 2016 (6 whole weeks)**, and **rest = 4 Jan to 22 May 2016 (20 whole weeks)**. Both are whole Monday-Sunday weeks, so each review cycle belongs to exactly one sub-period (assigned by the cycle's first demand day). We do not claim results generalise to other seasons.
- Closed days are known from the calendar, so the future-window closed-day count is a legitimate feature. A closed day is one where total store sales fall below a near-zero threshold (section 2), not `== 0`. It is identified as a calendar rule (e.g. Dec 25) from **training years only** (2011-2014, all before the tuning cutoff), never from sales inside the window being forecast, and then applied to 2015. In the simulator, closed days replay zero demand while holding cost still accrues.
- Calibration windows lag the regime. Version 1's calibration window (autumn, calm) is applied through the holiday peak, so sigma and conformal offsets will likely be too small there; the next version's window contains the peak and will be too large afterwards. **Post-hoc observation (see section 12):** the rest period also sits at a higher volume level than the summer calibration windows, which pushes the same way, so **under-service in the rest period is the expected effect for both policies**. Expect achieved-vs-target service gaps in both policies; conformal prediction carries no exchangeability guarantee for time series.
- **Model versions:** version 0 (cutoff 2015-10-25) serves the four warm-up review dates 25 Oct to 15 Nov; version 1 (cutoff 2015-11-22) serves 22 Nov to 10 Jan (its fit set ends 2015-08-30 = the tuning cutoff); version 2 (cutoff 2016-01-17) and version 3 (cutoff 2016-03-13) follow, and version 4 (cutoff 2016-05-08) serves only the last review date. Version 0's fit set ends 2015-08-02. The first versions were fit on data ending late July to late August 2015, so they have seen only four earlier holiday seasons (2011-2014).

## 6. Metrics (scaled, so series of different volume are comparable)

- **Point:** WAPE = sum|y - yhat| / sum y (pooled). **MASE** per series = test MAE / in-sample MAE of the naive-P forecast (`y_t - y_{t-P}`) on the training period, averaged over series. Series with zero scale are excluded and counted. Raw MAE/RMSE are reported for reference. MAPE is shown only on non-zero targets, to demonstrate that it breaks at zero, and is never used for selection.
- **Probabilistic:** **scaled pinball loss** = pinball loss divided by the same per-series scale as MASE, averaged over quantiles and series. Also **coverage**: empirical share of actuals inside [P10, P90] (nominal 0.80) and one-sided coverage of each quantile (share of `y <= q_alpha` vs alpha). One-sided coverage is what the reorder policy needs. Because demand is a small integer, nominal-or-higher coverage is expected for slow movers, and is explained rather than hidden.
- Evaluation origins are weekly (Sundays). **MASE scale** for a tuning fold is each series' in-sample MAE of the naive-P forecast over all daily origins whose target ended on or before the fold's first origin (nothing from the fold or later). **Point baselines have no quantiles**, so for them the one-sided figure reported is the share of actuals at or below the point forecast (the quantile level the forecast implicitly sits at), labelled as such; interval coverage is reported from Phase 7 on. **Selection rule:** models are selected on WAPE (point) and scaled pinball loss (quantile), never on MASE; MASE is reported as the mean over series plus the labelled median (the mean is fragile when a series has a tiny scale, Phase 5). MA-28 is the primary benchmark for every learned model.

## 7. Models

Baselines for the P-day sum: **naive** (previous P days), **MA-28** (28-day open-day mean x P, i.e. the 28-day mean scaled to the P-day window), **seasonal naive** (same window 364 days earlier, weekday-aligned; where that window does not exist because the series is under a year old it falls back to naive-P, and the share of fallback rows is reported). "Same weekday last week" collapses into naive for a 7-day sum, hence the swap. Then Linear Regression (numerics standardised, ids one-hot, predictions clipped at zero), Random Forest (training size bounded by a per-tree row cap and depth caps, section 12), XGBoost (squared error = mean forecast; early stopping on a temporal holdout with an embargo, section 12), XGBoost quantile (`reg:quantileerror`, LightGBM as fallback). ARIMA is dropped. Conformal calibration (split conformal on the calibration window, scores pooled by velocity segment as in section 5) is a cheap stretch, reported as coverage before vs after.

## 8. Inventory policies (all periodic-review, order-up-to level S)

Every policy sets S; order quantity = `max(0, S - inventory position)` (inventory position = on-hand + on-order). **S is rounded up with `ceil` to an integer for every policy** (same rule for all, so no policy benefits from the rounding; units are indivisible; a discrete quantile is the smallest integer whose CDF reaches the target).

Service-level grid for target-driven policies: **alpha in {0.80, 0.90, 0.95, 0.99}**.

- **Naive rule:** `S = ceil(c * mu28 * P)`, where `mu28` is mean daily sales over the 28 days ending at t and `c` is a cover multiplier. This is the "hold c times average demand over the protection interval" rule practitioners use. It has no service-level target, so it is swept over a grid of `c` (about 1.0 to 3.0, widened until its achieved service overlaps the other policies) to draw its own curve. `c = 1` is the plain "order up to average demand" point.
- **Point policy:** `S = ceil(max(0, yhat + z_alpha * sigma_s))`, where `yhat` is the squared-error XGBoost mean forecast for horizon P and `z_alpha` the normal quantile. `sigma_s = sigma_segment * scale_s`, where `sigma_segment` is the **RMSE of scale-normalised, out-of-sample residuals of the P-day forecast on the calibration window, pooled over all series in the velocity segment** (section 5); per-series estimates are too noisy. This is the textbook `z * sigma_daily * sqrt(P)`, with `sigma_daily * sqrt(P)` estimated directly at horizon P and pooled by segment. RMSE (not std) so any bias is charged to the policy. Assumes normal, independent demand: this is the assumption the quantile policy relaxes.
- **Quantile policy:** `S = ceil(q_alpha)`, the direct P-day quantile from the sorted quantile model (optionally conformally calibrated, with offsets pooled by segment as in section 5).
- Newsvendor link (for the README): ordering up to the `Cu/(Cu+Co)` quantile balances the cost of one unit too few against one too many. The service level is that critical ratio expressed as a probability.

## 9. Simulator

**Assumptions**

| Assumption | Choice | How it could be wrong |
|---|---|---|
| Demand | Actual M5 sales replayed | Understates true demand where the store was out of stock |
| Information | Planners see the true demand history even when simulated stock ran out (forecasts and the naive rule read historical M5 sales, not simulated sales) | Real systems only see censored sales; optimistic for all policies |
| Unmet demand | Lost sales | Backorder variant available as a sensitivity |
| Policy | Periodic review R = 7, order-up-to S, review weekday fixed to Sunday for all series | Real retailers stagger reviews by supplier |
| Lead time | Fixed L = 3 base (grid {3, 7}) | No supplier data; random lead time is a sensitivity |
| Timing | Order placed after close of day t, arrives before opening day t+L+1, so the protection interval is exactly days t+1..t+R+L, matching the target | |
| Start | Simulation starts at the review on Sunday 2015-10-25 with stock at S, using the **version-0 model**. The four warm-up cycles (25 Oct to 15 Nov) are excluded from metrics, so all 25 review dates from 22 Nov to 8 May count | Orders in flight at 22 Nov come from the warm-up; warm-up forecasts use the same rules as the test |
| Prices/costs | Unit price = item-store median price in the test window; unit cost = 70% of price; holding = 25%/year of unit cost per unit-day; stockout = lost margin (30% of price). Fixed before any results. | Illustrative; not from data |

**Headline result: trade-off curves at matched service levels.** For each policy (naive over `c`, point and quantile over alpha), plot **average on-hand inventory (units) against achieved service**. Two panels: **fill rate** (units served / units demanded, pooled; the business-facing metric and the primary panel) and **cycle service level** (share of review cycles without a stockout; what the quantile target alpha nominally controls, so also a calibration check of the decisions). Comparison = inventory needed to reach the same achieved service, linearly interpolated between points, no extrapolation, with the coarseness of a four-point grid stated. 95% confidence intervals from a **cluster bootstrap over items**: an item's three store-series are resampled together because they share the item's demand drivers and are not independent, so the effective sample size is 100 items, not 300. The bootstrap does not capture period-to-period variability (there is one 26-week test period), so the intervals understate uncertainty about other seasons; the README says so. Results are also broken down by velocity segment (high / mid / low, defined in section 2) and by the two test sub-periods (section 5).

**Cost metric is a sensitivity sweep, not the headline.** With the default costs, the cost-implied critical ratio is: holding per unit over one 7-day cycle = 0.7 x 0.25/365 x 7 x price = about 0.0034 x price; stockout = 0.3 x price; ratio = about 89, critical ratio about 0.989 (about 99%). So at default costs a cost-optimiser would simply choose the top of the grid, and a cost "win" would say more about the cost assumption than the policy. Instead we sweep the stockout-to-holding cost ratio rho over {4, 9, 19, 99}, whose critical ratios are {0.80, 0.90, 0.95, 0.99}, and report each policy's minimum total cost over its own settings at each rho. (Newsvendor critical ratio is a textbook approximation for periodic-review lost sales; the simulation is the arbiter.)

**Other sensitivities:** lead time L in {3, 7}; random lead time L ~ Uniform{2, 3, 4} (same mean 3 as the nominal, isolates variance; policies still plan with nominal L; orders may cross); no-future-price ablation; optional backorder variant; and a hindsight diagnostic, **"exclude zero-demand cycles"**: drop, from every policy's metrics alike, the review cycles in which the series' realised demand over the protection interval was zero. It is defined with hindsight, so it is a diagnostic of how much stock-out-like history distorts the comparison and never the headline.

**Reporting:** stockout rate, fill rate, average inventory, total cost, plus achieved-vs-target service. Written up even if quantile does not win everywhere.

## 10. Architecture

```
React (Vite) + Tailwind + Recharts
        |  REST/JSON
Node/Express -- validation, caching, reads precomputed results
   |                     | live calls: /recommend, /simulate
   v                     v
PostgreSQL          FastAPI (Python) -- trained XGBoost + SHAP explainer
 ^  (Docker locally,          ^
 |   Supabase to deploy)      |
 +-- offline pipeline (/ml): data -> features -> train -> backtest -> simulate -> write results
```

- Vite + React (not Next.js): Express is already the backend.
- Precompute forecasts, SHAP, backtest metrics and default simulations into Postgres; FastAPI serves only live what-ifs. Service-level and lead-time selectors snap to the trained grids (alpha in {0.80, 0.90, 0.95, 0.99}, L in {3, 7}); no interpolation between quantile models.
- Dashboard pages: Overview, Demand Forecast, Product Analysis (SHAP), Inventory, Risk/Alerts, Model Performance, Policy Simulator (clearly labelled as historical simulation with stated assumptions).

## 11. Scope

- **MVP:** everything above except the stretch items.
- **Stretch:** backorder variant, random-lead-time sensitivity, deployment. (Conformal calibration moved into Phase 7 as a reported variant, section 12.)
- **Out of scope:** price/promotion what-if sliders, weather APIs, drift monitoring, LSTM/Transformers, LLM chatbot, hierarchical reconciliation, ARIMA.

Environment: **Python 3.12** virtualenv with pinned requirements; Node 24; PostgreSQL via Docker.

## 12. Decision log

- 2026-09-20 Approved Phase 1 with amendments: (1) headline = matched-service-level inventory trade-off curve, cost as a sensitivity sweep since default costs imply about 99% critical ratio; (2) service-level grid {0.80, 0.90, 0.95, 0.99}, integer order-up-to levels (ceil, applied to all policies); (3) future-window covariate aggregates and stated known-prices assumption; (4) scaled metrics (WAPE, MASE, scaled pinball) and a target-normalization experiment; (5) point-policy sigma from out-of-sample calibration residuals, naive rule defined now; (6) Python 3.12.
- Choices made while writing this file (open to change): `ceil` rounding; primary matched-service panel = fill rate; RMSE (not std) for sigma; test window 26 weeks, calibration 12 weeks, retrain every 8 weeks; random lead time Uniform{2,3,4}; quantile set includes 0.10/0.50/0.90 for display alongside the four service-level quantiles.
- 2026-09-20 Second round of amendments: (1) model staleness is 12-20 weeks (12-week calibration gap plus up to 8 weeks of use), not 12; (2) velocity terciles and segments are computed once from data up to the tuning cutoff only, and used for sampling, pooling and reporting; (3) bootstrap is a cluster bootstrap over items, not series; (4) per-series sigma and conformal offsets are replaced by scale-normalised values pooled by velocity segment, for both policies; (5) test-window regime documented (holiday peak, closed-store days, regime-lagged calibration), with fixed sub-period reporting and a closed-day calendar feature.
- 2026-09-20 Phase 2 data verification (M5 files loaded; all counts, columns and dates match section 2) and resulting decisions, each logged separately:
  1. **Review calendar.** First review date is Sunday 2015-11-22; test demand days are 2015-11-23 to 2016-05-22; the review weekday is Sunday for all series; holiday peak = 23 Nov to 3 Jan (6 whole weeks), rest = 4 Jan to 22 May (20 whole weeks). The last usable review date is 2016-05-08 (14 days before the last sales day, because targets and simulation need the full P = 14 protection interval), giving 25 review dates common to all horizons. This replaces the earlier "about 22 Nov" wording, since exactly 26 weeks ending 22 May starts on Monday 23 Nov. Consequence: `test_start` is the first review date, so the tuning cutoff is Sunday 2015-08-30.
  2. **Closed days** are defined by a near-zero threshold on total store sales, not `== 0` (Dec 25 showed 5-8 units across CA_1..3 in every year 2011-2015). Threshold and closed-date list confirmed in Phase 3 from training years only (2011-2014), then applied to 2015 as a calendar rule.
  3. **Two-year history rule** uses the tuning-cutoff reading (2 years of history before the tuning cutoff, all three CA stores on sale), not "before the test window". With the 2015-08-30 cutoff from decision 1 this leaves **2,215** eligible items (2,221 was measured with a 31 Aug cutoff derived from a 23 Nov test start; 2,306 under the looser test-window reading). Enough to sample 100 items.
  4. **Documented data differences:** the sales file has a composite `id` column not listed originally; `calendar.csv` runs to 2016-06-19 (28 days past the last sales day) and those days are unused.
- 2026-09-20 Phase 3 (`ml/prepare.py`, `ml/eda.py`, figures in `docs/figures/`). Cleaning and subset, all derived from data <= 2015-08-30:
  - **Closed-day rule** = Dec 25: in 2011-2014 every CA store had a total below 1% of its median daily total on that date (max 7 units, against a minimum of 1,150 on any other training day), and no other date qualified. Applied to 2015 as a calendar rule. (2015-12-25 does show 0/2/4 units, consistent with the rule, but it was not an input.)
  - **Velocity segments:** series tercile cuts at 0.514 and 1.404 units/day (closed days excluded). Sampled items by stratum 33 low / 34 mid / 33 high, allocated across department x tercile cells (minimum 1 per cell, rest proportional); the resulting series segments are 99 low / 101 mid / 100 high. Departments: FOODS_3 25, HOUSEHOLD_2 19, HOUSEHOLD_1 18, HOBBIES_1 16, FOODS_1 10, FOODS_2 6, HOBBIES_2 6 items. Seed 42.
  - **Price gaps:** none. Every on-sale day of the 300 series has a `sell_price`, and no sale occurs without a price.
- 2026-09-20 Phase 3 findings that qualify earlier assumptions (open decisions in section 13):
  1. **Long zero runs look like stock-outs or delistings, not demand.** In training, 80 of 100 high-velocity series (3.45 units/day on average) have at least one zero run of 14+ days after their first sale (median run 28 days); about 11% of high-velocity days, 15% of mid and 34% of low sit inside such runs. This is limitation 2 (censored sales) measured: a series selling 3-4 units a day does not go 14 days without a sale by chance. As a result even 9% of 14-day windows are all-zero for the high segment (11% at 7 days), against 30% of days.
  2. **The holiday peak is small in volume.** For series on sale since mid-2011, mean daily units in 23 Nov to 3 Jan ran x1.00, 1.08, 1.01, 1.10 and 1.07 of the Sep-Oct level in 2011-2015. The main holiday effect on the data is the Dec 25 closure (finding 3 covers where the bigger level shift is).
  3. **Level in the test window is above the calibration-window level.** Series on sale since mid-2011 averaged 1.24 units/day in the 2015 calibration-like window (8 Jun to 30 Aug) and 1.43 (x1.16) in the holiday peak and 1.49 (x1.20) in the rest of the test window. In 2011-2014 the peak ran x0.91 to x1.13 and the rest x0.80 to x1.04 of the same summer window. So the regime lag section 5 warns about is real, but it is a spring-2016 level rise, not the holiday peak. Sigma and conformal offsets are scaled by the trailing mean, which absorbs part of it.
  4. **Weak, associational effects** (not causal): Saturday/Sunday index 1.23/1.19 vs about 0.88 midweek; FOODS units on SNAP days 2.23 vs 2.06 other days (SNAP days are 33% of days, and they cluster early in the month); on days priced 15%+ below the series median, sales index 1.38 vs 1.01 at the usual price, and 0.81 when priced 5%+ above. Prices change weekly; 82% of days are within 5% of the series median.
  5. **Intermittency:** daily zero share 80% (low), 55% (mid), 29% (high). Syntetos-Boylan classes: low 89 intermittent / 10 lumpy; mid 77 / 24; high 27 smooth / 10 erratic / 28 intermittent / 35 lumpy. Zero-window share falls with P: low 80% / 36% / 22% at P = 1 / 7 / 14. This supports the P-day-sum target.
  6. **Dead or near-dead series:** 28 of 300 series had zero sales in the 28 days before the tuning cutoff (12 high, 10 mid, 6 low; consistent with finding 1), and 1 series had zero sales over the whole test window. `FOODS_2_101_CA_2` has its first sale 1,578 days after its first price, so its first-price date is not a true launch.
- 2026-09-20 Decisions after Phase 3, each logged:
  1. **Stock-out runs:** the data stays unfiltered and limitation 2 is quantified (section 2). A "longest zero run in the last 91 days, capped at 56" feature is computed as of t (the cap of 56 is a Phase 4 choice, adjustable on the tuning folds only). Phase 10 reports a hindsight "exclude zero-demand cycles" diagnostic applied equally to all policies (section 9), not as the headline.
  2. **Cross-store check of zero runs** (training data, runs of 14+ open days; 2,125 runs; 19.9% of on-sale series-days are inside one). Simultaneity is far above independence: of run-days, 30.1% have only this store out (64.1% if independent), 29.5% two stores (31.9%), 40.4% all three (4.0%). 28.1% of runs never overlap another store's run, 61.9% overlap on at least half their days, 34.0% are fully overlapped. By segment, all three stores out on 34.7% (low), 46.9% (mid), 47.8% (high) of run-days. Reading: a large share is item-level (delisting, supply, or genuinely seasonal items, which are also zero everywhere at once) and a substantial minority is store-specific. This check cannot separate seasonal zeros from supply failures; that was not tested, so "stock-out" in Phase 3 finding 1 should be read as "stock-out, delisting or seasonal absence". No data was changed.
  3. **`FOODS_2_101_CA_2` is kept** under the pre-registered first-price rule and noted in section 2.
  4. **Warm-up:** the simulation starts at the 2015-10-25 review with a version-0 model, so all 25 review dates from 22 Nov to 8 May count in the metrics (sections 5 and 9). This replaces the earlier "4-week warm-up excluded" and resolves the Phase 10 question.
  5. **Disclosure about the test window:** the Phase 3 EDA looked at test-window volume (`weekly_level.png` and the level comparison by year include it). The holiday-peak / rest split (23 Nov to 3 Jan / 4 Jan to 22 May) was fixed before that and is **unchanged**. The spring 2016 level rise (about 1.2x the summer calibration-window level, against 0.80-1.04x in earlier years) is a **post-hoc observation**, not a pre-registered hypothesis; its expected effect is under-service in the rest period for both policies (section 5). From here on every modelling choice, including target normalization, windows and caps, is decided on the tuning folds only.
- 2026-09-20 Phase 5 pre-registration, logged **before any baseline score was computed** (folds in `ml/folds.py`, committed with this entry):
  1. **Tuning folds.** Four non-overlapping folds of 12 consecutive Sunday review origins each, all on data up to the tuning cutoff (the last fold's P = 14 targets end exactly on 2015-08-30): **F1** 2014-08-31 to 2014-11-16 (autumn, Thanksgiving in its targets); **F2** 2014-11-23 to 2015-02-08 (the holiday fold: the same calendar season as the test window's holiday peak, a year earlier, spanning Thanksgiving, Christmas with its closure, New Year and the Super Bowl); **F3** 2015-03-08 to 2015-05-24 (spring); **F4** 2015-05-31 to 2015-08-16 (summer, the season of version 1's calibration window). The same origins serve P = 7, 10 and 14. Chosen by a fixed rule (12 Sundays, last fold anchored to the cutoff, one fold aligned to the test window's calendar season) and not from any score. Contiguous except a three-week gap between F2 and F3.
  2. **What is run:** naive, MA-28 and seasonal naive on F1-F4 for P in {7, 10, 14}: WAPE (pooled, and by velocity segment), MASE (per-series scale as in section 6, zero-scale series excluded and counted), MAE, RMSE, and the share of actuals at or below the point forecast. Results are written to `docs/results/` with the commit that produced them.
  3. **No test-window metric is computed.** `folds.assert_tuning_only` enforces it in code (a fold whose target windows pass the tuning cutoff raises), `test_folds.py` tests the guard, and section 14 logs every touch.
  4. **Testing is part of the definition of done** for every phase from Phase 5 on (section 15, `docs/testing.md`), CI runs the fast suite.
- 2026-09-20 Phase 5 baseline results on the tuning folds (`docs/results/phase5_baselines_tuning.md`, run at code commit `4f1b04a`; tuning folds only, so **the test-window touch log (section 14) has no entry**). Means over the four folds, P = 7 / 14:
  - **WAPE:** MA-28 0.412 / 0.356, naive 0.454 / 0.371, seasonal naive 0.769 / 0.684. MA-28 has the lowest WAPE in 11 of the 12 fold x horizon cells (naive is lower in F4 at P = 14, 0.363 vs 0.368). By velocity segment WAPE is highest for the low segment for every baseline (MA-28 at P = 7: 0.69 low, 0.48 mid, 0.37 high).
  - **Seasonal naive is much worse, and it is not a bug** (its features pass the brute-force test, no row used the fallback). The year-ago window over-forecasts by 2-23% across the folds (volume drifted down, section 12 Phase 3 finding), it correlates less with the target than last week does (0.50-0.71 vs 0.78-0.89 at P = 7), and after rescaling away the bias its WAPE stays 0.62-0.81. Read: a series' own year-ago week is a weak predictor here; the `sum_364_P` feature may carry little, to be judged on the tuning folds.
  - **The holiday fold F2 is the highest or tied-highest WAPE for naive and MA-28 at P = 7 and 10**, by margins of 0.01-0.04. Not a large regime effect at the baseline level.
  - **MASE, mean vs median.** Mean-over-series MASE (the section 6 definition, unchanged) is fragile: in F4 it is dominated by `FOODS_2_101_CA_2` (naive scale 0.03, MASE 217; first sale 1,578 days after first price, kept under the pre-registered rule). Without the five worst series the F4 naive P = 14 mean falls from 1.70 to 0.93. A median-over-series column was added as a robustness display after seeing this (a post-hoc addition to the reporting, not to the definition); both are reported. 298-300 series enter MASE per fold and horizon (0-2 zero-scale series excluded).
  - **Implied quantile level:** the share of actuals at or below the point forecast is 0.57-0.64 for all three baselines: mean-type forecasts of skewed, intermittent demand sit above the median, at about the 60th percentile. This is the level a service-level target has to be compared with, not evidence of calibration.
- 2026-09-20 **Phase 6 pre-registration**, committed before any learned model was scored. Rules for choosing and reporting models, all decided on the tuning folds only:
  1. **Selection metric, all phases.** Point models are selected on mean WAPE; quantile models on mean scaled pinball loss. MASE (mean, with the labelled median column) is reported, never used for selection. **MA-28 is the primary benchmark**: every learned model is reported against it (WAPE, WAPE ratio to MA-28, MASE mean and median, share of actuals at or below the forecast) per fold and horizon, with **F2 (the holiday fold) reported separately** next to the four-fold mean. F2 is inside the mean used for selection and is also shown on its own.
  2. **Tuning mirrors deployment.** For each fold F with first origin c_F there is one model version per horizon and config with cutoff c_F: fit set = origins with s + P <= c_F - 84 days (`versions.fit_mask`), a 12-week calibration window [c_F - 84 days, c_F] (`versions.calibration_mask`) that is **reserved and not used for fitting, early stopping or selection in Phase 6**, and evaluation on the fold's 12 Sunday origins. Staleness is therefore 12 to 23 weeks (the fold is 12 weeks long, deployment versions serve 8), slightly more pessimistic than deployment. Fit sets use all daily origins with at least 91 days on sale.
  3. **Search space and budget (fixed; nothing is added after scores are seen).** One config per family and target variant, chosen on the mean of the 12 cells (F1-F4 x P in {7, 10, 14}), not per horizon. Seeds fixed at 0.
     - **Linear Regression:** no hyperparameters. Numeric features standardised (mean and std from the fit set), item/store/dept/cat one-hot (unseen levels ignored), missing `sum_364_P` filled with the fit-set median plus a missing-indicator, ordinary least squares, predictions clipped at 0.
     - **Random Forest:** 100 trees, `max_features` 0.5, bootstrap with **`max_samples` = 20,000 rows per tree**. Grid: `max_depth` in {10, 16} x `min_samples_leaf` in {20, 60} = 4 configs. Training size is bounded by the per-tree row cap and the depth cap; no other subsampling, all fit-set rows are eligible. Ids enter as ordinal codes (arbitrary order).
     - **XGBoost (`reg:squarederror`, `hist`):** learning rate 0.05, subsample 0.8, colsample_bytree 0.8, lambda 1, at most 1,000 rounds. Grid: `max_depth` in {3, 6} x `min_child_weight` in {5, 30} = 4 configs. Ids as ordinal codes.
     - **Budget:** LR 2 variants x 12 cells; RF and XGBoost 4 configs x 2 variants x 12 cells each, plus 3 floor candidates x 12 cells (below). Selection: lowest mean WAPE; if two configs are within 0.002 absolute, take the simpler (smaller `max_depth`, then larger `min_child_weight` or `min_samples_leaf`).
     - The four folds are used both to select and to report, so reported fold scores are mildly optimistic. The test window is untouched. Anything other than a bug fix that changes a rule above must be logged here before it is run again.
  4. **Early stopping (XGBoost only), no random validation split.** Within a fold model's fit set, with L the last fit origin: the validation set is the fit origins after L - 56 days (last 8 weeks) and the early-stopping stage trains on fit origins with date + P <= L - 56 days, so no training target day is a validation target day (embargo). Stop after 50 rounds without improvement in squared error on the training target scale (raw or normalised). Then refit on the entire fit set with `best_iteration + 1` rounds. If a frame has no `date` column (synthetic tests) a fixed round count is used instead.
  5. **Normalisation A/B and scale floor.** `scale(s, t) = max(mean_28, floor) * P` with `mean_28` the open-day 28-day mean at origin t; train on `Y / scale`, predict `yhat * scale`, clip at 0. **Floor:** chosen first from {1/56, 1/28, 1/14} units per day, using XGBoost at depth 6 and `min_child_weight` 30 with normalisation on, lowest mean WAPE over the 12 cells (ties go to 1/28); the chosen floor is then fixed for every family. **A/B rule: SUPERSEDED before any scoring** (the original text was: adopt the normalised target iff its mean WAPE is lower than raw by at least 0.005). See the amendment entry below: Phase 6 makes no final normalisation decision.
  6. **Year-ago feature ablation (`sum_364_P`).** After selection, each family's selected config is refit without `sum_364_P` on the same 12 cells. The feature is **kept only if it lowers XGBoost's mean WAPE by at least 0.003 absolute**; otherwise it is removed from `features.columns` for all models. F2 is reported separately for all three families, and the other two families are informational. **Amended before any scoring:** run for both target variants, decided per variant; see the amendment entry below.
  7. **Phase gate and test record.** No skipped placeholder tagged for a phase may remain when that phase is declared done, and the fast and real-data test results are re-run and recorded at each phase's commit (section 15).
  8. Test-window touch log: unchanged, no entry expected. Phase 6 runs only through `folds.select` and `versions.fit_mask` for origins whose targets end by 2015-08-30.
- 2026-09-20 **Amendment to the Phase 6 pre-registration, before any learned model was scored** (nothing in Phase 6 had been run; no scores existed):
  1. **Normalisation is decided in Phase 7, on scaled pinball, not in Phase 6.** The earlier text made the raw-vs-normalised call in Phase 6 on WAPE with a 0.005 margin; that rule is withdrawn. Instead Phase 6 fits both variants for every point family (each with its own best config, chosen by mean WAPE as before) and **reports both side by side: WAPE, MASE (mean and the labelled median), WAPE by velocity segment, F2 separately, and each against MA-28**. No variant is declared the winner. Both variants' selected configs are carried into Phase 7, which makes the **single, final normalisation decision for the whole pipeline (point and quantile models alike) on mean scaled pinball over the tuning folds**, under a rule that will be committed to this file before any Phase 7 scoring. Until then downstream code must not assume either variant. The scale floor is chosen in Phase 6 (as pre-registered, on XGBoost WAPE) as a provisional value of the normalised variant, and Phase 7 may re-test it on scaled pinball under that same rule.
  2. **Feature ablations are run for both variants and decided per variant.** The year-ago (`sum_364_P`) rule is unchanged (kept only if it lowers XGBoost's mean WAPE by at least 0.003), applied within each variant.
  3. **New: id-features ablation for Random Forest and XGBoost.** Ids enter the trees as arbitrary ordinal codes and `item_id` lets a model memorise items, so the value of the id features is tested. At each variant's selected config, each of the two families is refit on the same 12 cells with three arms: **all four ids** (item, store, dept, cat; the default), **no `item_id`** (store, dept, cat kept), and **no ids**. The arm with the lowest mean WAPE wins; an arm with fewer ids is preferred if the arm with more ids is not better by at least 0.003 absolute (simplicity order: no ids, then no `item_id`, then all four). Decided per family and per variant, F2 reported separately. Linear Regression is excluded: its one-hot ids are part of its definition. Budget: 2 extra arms x 12 cells x 2 variants x 2 families.
  4. **Reporting for raw vs normalised** is as in item 1; the Phase 6 results file has one block per variant so the comparison is visible without opening the CSV.
- 2026-09-20 **Phase 6 results: LR, RF and XGBoost point forecasts on the tuning folds** (`docs/results/phase6_point_tuning.md`, run at code commit `54fb2f4`; every rule was pre-registered above and committed before the first score; tuning folds only, **touch log unchanged**). Both target variants are reported side by side and **no normalisation decision is taken here** (Phase 7, on scaled pinball). Means over the 12 fold x horizon cells; MA-28 is 0.385.
  - **Scale floor** (XGBoost, normalised): mean WAPE 0.5447 at 1/56, 0.4415 at 1/28, 0.4015 at 1/14 units/day, so **1/14 is chosen, the upper edge of the pre-registered candidates**. A larger floor might do better still; the grid is not extended (rule fixed before scoring), and Phase 7 may re-test the floor.
  - **Selected configs** (mean WAPE): raw: LR 0.3854, RF depth 10 / leaf 20 0.3693, XGBoost depth 3 / min_child_weight 30 0.3761 (depth 6 / 30 was 0.3751, inside the 0.002 tie, so the simpler one is taken); normalised: LR 0.5268, RF depth 16 / leaf 60 0.3774, XGBoost depth 3 / 30 0.4023. **Hyperparameters barely matter**: the four RF configs span 0.3692-0.3721 (raw) and the four XGBoost configs 0.3751-0.3763. Selection and reporting use the same four folds, so fold scores are mildly optimistic.
  - **Against MA-28, raw target, P = 7 / 10 / 14 (WAPE ratio, below 1 is better):** RF 0.962 / 0.957 / 0.958, better than MA-28 in all 12 cells; XGBoost 0.985 / 0.967 / 0.978, better in 11 of 12 (worse at P = 7 in F3); LR 1.003 / 0.994 / 1.006, a tie. **F2 (holiday fold)** ratios: RF 0.966 / 0.953 / 0.951, XGBoost 0.977 / 0.952 / 0.968, LR 0.993 / 0.979 / 1.002, no weaker than the other folds.
  - **The WAPE gain comes from mid and high velocity series and is not visible per series.** In the low segment every learned model has a *higher* WAPE than MA-28 at every horizon (P = 7: LR 0.716, RF 0.707, XGBoost 0.752 vs MA-28 0.689). On the series-equal-weight metrics the picture is flatter: mean MASE for RF 0.978 / 1.012 / 1.115 vs MA-28 0.987 / 1.015 / 1.145, XGBoost 1.000 / 1.015 / 1.161 (no better); median MASE RF is slightly better (0.756 / 0.733 / 0.770 vs 0.769 / 0.752 / 0.785), XGBoost and LR are worse than MA-28 at every horizon. Summary: a modest, volume-weighted gain for RF, a smaller one for XGBoost, none for LR.
  - **Normalised vs raw (reported, not decided):** WAPE ratio to MA-28 for the normalised variants is RF 0.982 / 0.977 / 0.982, XGBoost 1.038 / 1.043 / 1.054, LR 1.216 / 1.347 / 1.567. No normalised family beats its raw counterpart on WAPE, and on MASE they are equal or worse. This is the WAPE view only; the decision belongs to Phase 7 on scaled pinball, where normalisation may behave differently.
  - **Ablations, decided per variant under the pre-registered rules.** Year-ago `sum_364_P` (XGBoost): raw 0.3761 with vs 0.3774 without, a gain of 0.0013 < 0.003, so **drop** for the raw variant; normalised 0.4023 vs 0.4065, a gain of 0.0042, so **keep** for the normalised variant. Ids (RF and XGBoost, both variants): **no id features** in all four cases, because more ids never beat fewer by 0.003 (differences at most 0.0015; XGBoost raw with all ids is 0.3761 vs 0.3775 with none, and slightly better in F2, 0.3855 vs 0.3891). These outcomes are **recorded, not yet applied**: `learned.py` defaults and `features.columns` are unchanged until Phase 7 fixes the variant; RF and XGBoost point models will then drop the ids, and the year-ago column follows the chosen variant.
  - **Bug found by the tests:** RF predictions were not bit-reproducible (parallel tree summation order); prediction now runs single-threaded (`n_jobs = 1`). Also `tests/test_versions.py` now checks that each tuning fold's model version mirrors deployment.
- 2026-09-20 **No-future-price ablation, report-only** (the known-prices assumption of section 4). It was **not** part of the Phase 6 pre-registration and was run **after Phase 6 was declared done** (runner commit `37c1322`); it has no threshold and informs no decision. Each family was refit at each variant's selected config without `price_mean_rel_p{P}` and `price_min_rel_p{P}` on the same 12 cells. Mean WAPE with -> without the future-price columns: raw target: LR 0.3854 -> 0.3853, RF 0.3693 -> 0.3702, XGBoost 0.3761 -> 0.3786; normalised target: LR 0.5268 -> 0.5213 (removing them helps LR), RF 0.3774 -> 0.3789, XGBoost 0.4023 -> 0.4048. Known future prices therefore lower WAPE by 0.0009 (RF) and 0.0025 (XGBoost, 0.7% relative) on the raw target. Without them RF (0.3702) and XGBoost (0.3786) on the raw target are still below MA-28 (0.3851), so the trees' modest gain over MA-28 does not depend on the known-prices assumption. Touch log unchanged.
- 2026-09-20 **Phase 7 pre-registration (quantile models, normalisation decision, coverage, conformal)**, committed before any quantile model is scored. Tuning folds F1-F4 only, one model version per fold and horizon exactly as in the Phase 6 entry (fit set ends at c_F - 84 days, reserved 12-week calibration window, 12 Sunday evaluation origins). Numbers marked (chosen) are my proposals, fixed here before scoring.
  1. **Quantile model.** XGBoost `reg:quantileerror` with `quantile_alpha` = {0.10, 0.50, 0.80, 0.90, 0.95, 0.99} fitted jointly (one multi-quantile model per horizon and fold version), output sorted along the quantile axis (monotone rearrangement) and clipped at 0; `predict` returns the median. Registered as `xgb_q` (kind "quantile"). The share of rows with crossing before sorting is reported. LightGBM is used only if multi-quantile fitting fails, and that is logged. Same 4-config grid as the point model (`max_depth` in {3, 6} x `min_child_weight` in {30, 5}), learning rate 0.05, subsample 0.8, colsample_bytree 0.8, at most 1,000 rounds, early stopping (50 rounds) on the Phase 6 temporal holdout with embargo using XGBoost's quantile loss over the six alphas, then a refit on the whole fit set with `best_iteration + 1` rounds. One config per variant, chosen on mean scaled pinball over the 12 cells; configs within 0.5% relative (chosen) of the best: take the simpler.
  2. **Metrics and benchmarks.** Scaled pinball = per-series mean pinball / the same per-series naive-P scale as MASE (section 6), averaged over series (zero-scale series excluded and counted) and over the six quantiles; reported as the **mean over series and the labelled median over series**, per alpha for the four service quantiles, by fold, F2 separately, by segment and by horizon. **Benchmarks** (ratio below 1 is better): the point policy's quantile of section 8, `q_alpha = yhat + z_alpha * sigma_seg * scale`, built from (B1) the MA-28 forecast and (B2) the Phase 6 XGBoost mean forecast on the raw target; `sigma_seg` is the RMSE of scale-normalised residuals of the same forecast over the version's calibration window (Sunday origins), pooled by velocity segment; normal `z_alpha`, clipped at 0. The same window, pooling and scale are used for the conformal offsets, so no method gets better-calibrated inputs.
  3. **Normalisation decision (final, whole pipeline, point and quantile models alike), on scaled pinball.** Candidates: the raw target, and the normalised target with floor in {1/28, 1/14, 1/7} units/day (1/56 is dropped after Phase 6, where it was clearly worst; **1/7 is added on purpose** because the best floor in Phase 6 was the top edge of its range). Step A, floor: XGBoost quantile at depth 3 / `min_child_weight` 30, normalised, lowest mean scaled pinball over the 12 cells; floors within 0.5% relative are tied and go to 1/14. Step B: per-variant config selection as in item 1, the normalised variant using the chosen floor. Step C, decision: **adopt the normalised target only if, at the selected configs, it beats the raw target by at least 1% relative (chosen) in both the mean over series and the median over series of scaled pinball**, each averaged over the 12 cells; otherwise raw. Requiring both guards against one tiny-scale series driving the mean (Phase 5 and 6: `FOODS_2_101_CA_2`, MASE 217). Not part of the decision, but reported: WAPE and MASE (mean and median) of the median forecast, coverage, F2 separately, low-velocity segment separately. The Phase 6 point models follow the chosen variant, and the Phase 6 provisional floor is superseded by Step A.
  4. **Feature ablations for the quantile model, per variant, at the selected config.** Ids (three arms as in Phase 6): fewer ids preferred unless a larger set lowers mean scaled pinball by at least 0.5% relative. Year-ago `sum_364_P`: kept only if it lowers mean scaled pinball by at least 0.5% relative. Future-price columns: report-only. **Amended before scoring (see the amendment entry below): the ids and year-ago ablations are report-only sensitivities; the feature set follows the adopted variant and is shared by both policies.** The Phase 6 point-model outcomes (ids removed; year-ago follows the chosen variant) apply to both policy models once the variant is fixed.
  5. **Coverage, tolerance and discrete-quantile reporting.** The quantile policy orders `S = ceil(q_alpha)`, so coverage is evaluated on `ceil(q)`. For alpha in {0.80, 0.90, 0.95, 0.99}, per fold, horizon and segment, and F2 separately, report `cov_hi` = share(y <= ceil q), `cov_lo` = share(y < ceil q), the tie share `cov_hi - cov_lo`, the share of forecasts with `ceil q = 0`, and the coverage of the raw `q` for reference. Status per cell: **under-covered if `cov_hi` < alpha - 0.03; over-covered if `cov_lo` > alpha + 0.03; otherwise consistent with calibration** (the nominal level is reached somewhere within the discrete step). The 0.03 tolerance (chosen) is about four standard errors at 3,600 forecasts plus room for the dependence between weekly origins; it is not tuned. The 80% display interval is reported as the share of y in [floor(q10), ceil(q90)]. Results are reported whether or not they meet nominal; nothing is tuned to hit coverage, and 0.99 is a tail estimate, not assumed to work.
  6. **Conformal calibration** is built and evaluated in Phase 7 (moved forward from the stretch list) as a reported **variant**; the headline quantile policy stays the uncalibrated `ceil(q)`.
     - Split conformal per model version on its reserved calibration window; calibration origins are the **Sunday** origins in `versions.calibration_mask` (targets observed by the cutoff, every target day after every fit target day). Daily origins are not used (overlapping, dependent).
     - Score for level alpha: `s = (y - q_alpha(x)) / scale(s, t)`, `scale = max(mean_28, floor) * P` with the frozen floor from item 3 (whichever target variant wins). Positive when the forecast was too low.
     - Pooled by velocity segment (labels fixed, section 2), never per series alone. With n pooled scores, `k = ceil((n + 1) * alpha)` and `offset_seg,alpha` is the k-th smallest score. Calibrated quantile `q~_alpha = max(0, q_alpha + offset_seg,alpha * scale(s, t))`, then re-sorted across alphas.
     - **Too-few-scores guard:** a level needs `n >= n_min(alpha) = ceil(alpha / (1 - alpha))` (so that `k <= n`): 4, 9, 19, 99 for 0.80, 0.90, 0.95, 0.99. If `n < n_min` the code **raises `InsufficientScores`**; it never returns the sample maximum or a silently uncalibrated value, and the report shows "not calibrated (n = ...)" for that segment and level. A thin-sample warning (reported, not an error) fires when `n < 5 / (1 - alpha)`: 25, 50, 100, 500. Per series there are about 12 calibration origins, which cannot support a 0.99 offset at all, hence the pooling; pooled per segment there are about 1,200.
     - Coverage before and after is reported with the item 5 table on the fold's evaluation origins, by fold, F2 and segment. No exchangeability guarantee holds for time series, and the calibration window precedes the evaluation origins (regime lag), so gaps are expected and reported.
  7. **Placeholders and gates Phase 7 must clear.** `tests/test_conformal.py` is retagged from Phase 8 to **Phase 7** and must be replaced by the tests in `docs/testing.md#conformal` before Phase 7 is declared done. `xgb_q` must be registered (`REQUIRED_MODELS[7]`), which subjects it automatically to the contract tests and the synthetic quantile-truth tests. New pure-function tests are also required, each with hand-computed cases: the relative-tolerance selection rules, `cov_lo` / `cov_hi` / status including the boundaries, the benchmark quantile `yhat + z * sigma * scale`, and the normalisation rule (including a case where the mean and the median disagree, which must stay raw). `ml/record_tests.py 7` must record a passing fast and real-data run before the phase is declared done.
  8. **Test window:** no quantile model is scored on it; the touch log stays as is. As in Phase 6, any change to a rule above after scoring begins is limited to bug fixes, and each is logged here.
- 2026-09-20 **Phase 6 cells won and item-cluster bootstrap against MA-28** (report-only, added after Phase 6 closed; `ml/bootstrap_point.py`, `docs/results/phase6_point_bootstrap.md`). Raw target, frozen configs, tuning folds only, touch log unchanged. The refit WAPEs reproduce the logged Phase 6 values to 1e-9. Items (100), not series (300), are resampled, each item's three store-series together; 10,000 resamples, seed 0. Pooled WAPE difference to MA-28 (model minus MA-28, negative is better), with 95% interval: **RF wins 12 of 12 cells, -0.0157 [-0.0228, -0.0085]; XGBoost wins 11 of 12 cells, -0.0091 [-0.0164, -0.0010]; LR wins 6 of 12, +0.0004 [-0.0090, +0.0118].** RF's interval excludes zero at every horizon and in F2 (-0.0175 [-0.0287, -0.0052]). XGBoost's is weaker: it excludes zero overall, at P = 10 and at P = 14 (upper end -0.0000) and in F2 over all horizons (-0.0137 [-0.0258, -0.0004]), but **includes zero at P = 7 (-0.0064 [-0.0140, +0.0019]) and in F2 taken at P = 7 or P = 14 separately**. So RF's edge over MA-28 is clear on item sampling, XGBoost's is small and borderline, and LR has none. The interval reflects item sampling only, not period-to-period variability (one set of folds), so it understates uncertainty about other periods.
- 2026-09-20 **Amendment to the Phase 7 pre-registration, before any quantile model was scored** (nothing in Phase 7 had been run):
  1. **Both policies use XGBoost; Random Forest is reported only as a comparison.** The point policy takes its forecast from the XGBoost squared-error mean model and the quantile policy from XGBoost quantile regression (sections 7-8). One model family for both policies means the policies differ only in how uncertainty enters (normal quantile around a mean vs a direct distribution-free quantile). RF appears in Phase 7 tables only as a comparison model (its point forecasts, benchmarked against the same references) and feeds no policy. **Disclosure:** in Phase 6 RF was more accurate than XGBoost on WAPE (12-cell mean 0.3693 vs 0.3761 raw), so the point policy's forecast is not the most accurate one available; that is accepted for the sake of a like-for-like policy comparison.
  2. **The feature set follows the adopted target variant, and both policies share it.** In Steps A-C each variant is evaluated with its own Phase 6 recorded feature set: **raw = no ids, no `sum_364_P`; normalised = no ids, with `sum_364_P`** (future-price and all other columns kept in both). Once Step C adopts a variant, its set is the base set for **both** XGBoost policy models, so the year-ago column is kept **only if the normalised variant wins**. This replaces the quantile-specific id and year-ago decisions of item 4 of the Phase 7 pre-registration: those two ablations are still run at the adopted variant but are **report-only sensitivities** and change no feature set.
  3. **Feature-list check and two candidate feature groups, pre-registered as tuning-fold ablations.** Checked against `features.columns`: the existing event inputs are counts only (`n_event`, `n_sporting`, `n_cultural`, `n_national`, `n_religious`, plus SNAP, weekend and the Dec 25 closed-day count), so **there are no event-specific (named-event) indicators**, and **there is no cross-store feature** (`store_id` is only an identifier); the cross-store zero-run feature deferred in section 12 (decision after Phase 4) is therefore still open. Both are pre-registered now:
     - **(a) Named-event indicators (`ev_named`).** For each named event (`event_name_1` or `event_name_2`) with at least 3 occurrences on or before the tuning cutoff (2015-08-30), one column `ev_<name>_p{P}` = 1 if an occurrence falls in t+1..t+P. All 30 qualify (each has 4 or 5 occurrences): Easter, Cinco De Mayo, Eid al-Fitr, LentWeek2, IndependenceDay, Father's day, LentStart, Purim End, OrthodoxEaster, Pesach End, NBAFinalsStart, NBAFinalsEnd, Mother's day, MemorialDay, SuperBowl, ValentinesDay, Ramadan starts, StPatricksDay, PresidentsDay, Christmas, ColumbusDay, Chanukah End, MartinLutherKingDay, LaborDay, EidAlAdha, Halloween, NewYear, OrthodoxChristmas, Thanksgiving, VeteransDay. Leakage note: the event calendar is public in advance; the list is fixed from calendar dates alone, never from sales.
     - **(b) Cross-store zero run (`xs_zero`).** Two columns: `other_zero_run_91` = the mean, over the item's other two stores, of their capped longest zero run in the last 91 days (the existing `zero_run_91`, cap 56), and `other_zero_28` = the number of the other stores (0 to 2) with zero sales over the last 28 open days, both as of t. Leakage note: other stores' history on or before t only, known at order time; missing (NaN) if an item has fewer than three stores. Motivation: zero runs are strongly simultaneous across an item's stores (section 12, Phase 3 follow-up), so a store's sibling stores are informative about item-level absence.
     - **Rule.** After Step C, at the adopted variant and its selected config, the XGBoost quantile model is refit with each group added separately to the base set (no re-search of hyperparameters). A group is **added only if it lowers mean scaled pinball over the 12 cells by at least 0.5% relative**; every group that passes is added; both policy models then share the resulting set. F2 is reported separately, and WAPE of the median forecast is reported alongside, informationally. Both feature builders need brute-force reference and sales-perturbation leakage tests (as in `tests/test_features.py`) and `docs/features.md` entries before any fit uses them.
  4. **New gate items:** the tests for `ev_named` and `xs_zero`, and the shared-feature-set rule of item 2 (the base set follows the adopted variant), join the Phase 7 tests required in item 7 of the Phase 7 pre-registration.
- 2026-09-20 **Execution amendment to the Phase 7 pre-registration, before any Phase 7 score was computed or viewed.** A timing probe of the first four quantile fits (floor stage) reported only fit seconds, rounds and whether the round cap bound: **early stopping ran to 999-1,000 of the 1,000-round cap in all four, about 130 s per fit** on 16 threads. No scaled pinball, coverage or WAPE was displayed, and the probe's cache was discarded. Consequences, none of which changes a pre-registered rule:
  1. **The cap of 1,000 rounds at learning rate 0.05 stays**, and is likely to bind (the 0.99 quantile converges slowly, as the synthetic tests showed). The runner records `hit_cap` per fit and the report counts them. As a **report-only check** the final config is also refit at learning rate 0.1 with the same cap and shown next to the pre-registered result, so the effect of an unconverged model is visible; it changes no decision.
  2. **Execution:** the fits are sharded over processes, each writing its own cache and all merged on start. Thread scaling is poor (200 rounds: 15 s on 16 threads, 46 s on 2, so 2-thread shards would give no gain), and memory limits the process count (one shard uses about 1.3 GB, reduced by holding one fold cell at a time). **Every XGBoost fit uses the same fixed thread count (`XGB_THREADS`, 5, with 3 shards on 16 cores)** so results are reproducible. Cell assignment to shards is deterministic and does not affect any fit.
  3. **Order:** the decision-critical stages run first (A floor, B search, C decision, D feature groups, E final models, benchmarks and conformal); the report-only sensitivities (ids, year-ago flipped, future price removed, learning rate 0.1) run last.
- 2026-09-21 **Phase 7 Step A result, and a pause.** Step A (scale floor; XGBoost quantile, depth 3 / `min_child_weight` 30, normalised) under the pre-registered rule, mean scaled pinball over the 12 cells: **0.2739 at 1/28, 0.2691 at 1/14, 0.2664 at 1/7 units/day**. 1/14 is 1.0% behind the best, outside the 0.5% tie band, so **the floor is 1/7**. As in Phase 6, the chosen floor is the **top edge of the candidate range**, so a larger floor might do better still; the grid is not extended (rules fixed before scoring) and this is a stated limitation of the normalised variant. All 36 fits are cached (5 XGBoost threads each, 3 shards). Two operational notes with no effect on any result: the first sharded run crashed after saving its fits because each shard printed a summary over all cells while holding only its own results (fixed: summaries are skipped unless all shards are merged in, commit `4881383`), and the run was **paused by the user after Step A** and resumed on 2026-09-21 from the cache, which skips finished fits.
- 2026-09-21 **Phase 7 Steps B and C under the pre-registered rules** (read from the cached fits; tuning folds only, touch log unchanged). Step B selected depth 6 / `min_child_weight` 30 for both variants (mean scaled pinball, raw 0.2839 vs 0.2854-0.2874 for the other three configs; normalised at floor 1/7 0.2639 vs 0.2653-0.2668). **Step C: the normalised target is adopted**: mean scaled pinball 0.2639 vs 0.2839 (-7.0%) and median over series 0.1861 vs 0.2049 (-9.2%), both far beyond the required 1% relative. The feature set therefore follows the normalised variant (no ids, year-ago kept). **Caveat, added after seeing this result:** the 1,000-round cap binds much harder on the raw target (9 to 12 of 12 raw fits hit it, against 4 to 12 of 12 normalised, and 5 of 12 for the selected normalised config), so part of the gap may be under-convergence of the raw fits. A **report-only** refit of the selected raw config at learning rate 0.1 (cap 1,000) is therefore added to the sensitivities stage, next to the pre-registered comparison. It cannot change the decision, which is fixed by the rule as written.
- 2026-09-21 **Phase 7 results: Steps D and E** (`docs/results/phase7_quantile_tuning.md`; tuning folds only, touch log unchanged; the report-only sensitivities were still running when this was written and get their own entry). Adopted: normalised target, floor 1/7, depth 6 / `min_child_weight` 30.
  1. **Step D, feature groups (rule: add iff mean scaled pinball falls by at least 0.5% relative):** named-event indicators `ev` -0.42% (F2 +0.07%, i.e. slightly worse), cross-store zero run `xs` -0.02% (F2 +0.20%). **Neither is added** (the events group misses the bar narrowly). The shared feature set for both policy models is the normalised variant's base set: no ids, year-ago kept.
  2. **Headline, mean scaled pinball over the 12 cells (lower is better):** XGBoost quantile **0.2639** (median over series 0.1861); point-policy benchmark B2 (XGBoost mean + normal sigma, section 8) 0.4128; B1 (MA-28 + normal sigma) 0.4230; RF mean + normal sigma (comparison only) 0.3974. Ratio of the quantile model to B2 is **0.639**, to B1 0.624 *[read with the framing rule of 2026-09-21: never quoted alone]*. **F2 (holiday fold):** 0.2315 vs B2 0.3858 (ratio 0.600), B1 0.4017, RF 0.3764. By horizon (quantile vs B2): P = 7 0.2464 vs 0.3739, P = 10 0.2602 vs 0.4077, P = 14 0.2851 vs 0.4568. **By velocity segment the advantage is very uneven:** low 0.3564 vs 0.3920 (ratio 0.91), mid 0.2252 vs 0.2957 (0.76), high 0.2116 vs 0.5517 (0.38). By quantile level (quantile model vs B2): 0.1: 0.1869 vs 0.2547; 0.5: 0.4966 vs 0.5371; 0.8: 0.4016 vs 0.7121; 0.9: 0.2693 vs 0.5262; 0.95: 0.1672 vs 0.3407; 0.99: 0.0621 vs 0.1062. The median forecast of the quantile model has WAPE 0.3579 (MA-28 0.3851; Phase 6 mean models 0.369-0.376).
  3. **Coverage of `ceil(q)`** (discrete-aware, tolerance 0.03). **Quantile model: `consistent` in all 12 cells at all four service levels** (0 under, 0 over). Mean `cov_hi` / `cov_lo`: 0.851 / 0.791 at 0.80, 0.922 / 0.890 at 0.90, 0.961 / 0.944 at 0.95, 0.996 / 0.994 at 0.99; F2 the same pattern. The 80% display interval covers 0.871 (nominal 0.80, above it as expected for integer demand). By segment the low-velocity series are covered more generously (`cov_hi` 0.903 at 0.80) than high-velocity (0.809). **Point policy B2 is over-covered at 0.80 (`cov_lo` 0.925) and 0.90 (0.957) in all 12 cells**, and consistent at 0.95 and 0.99: its normal quantiles around a mean, with sigma pooled by segment as RMSE, are too wide in the middle of the distribution, which is where most of its pinball loss comes from. The status test is weak where the tie mass is large (0.06 at 0.80), so "consistent" means "not contradicted".
  4. **Conformal variant gives no benefit here:** mean scaled pinball 0.2659 vs 0.2639 (+0.8%), median 0.1850 vs 0.1861 (-0.6%), coverage practically unchanged (F2 `cov_hi` at 0.90 0.922 vs 0.925). No segment and level was short of scores (minimum n 1,089, median 1,111, no thin-sample warning), so the guard never fired. The uncalibrated quantile model already sits inside the calibration band on these folds.
  5. **Fairness of the point-policy comparison, checked and reported.** Adopting the normalised target for the whole pipeline makes the shared-set point model (B2) less accurate than the frozen Phase 6 raw XGBoost on WAPE (0.3899 vs 0.3761). A **report-only** benchmark B2' (frozen Phase 6 raw XGBoost, all Phase 6 features, plus the same normal-sigma method, added after seeing the result) scores 0.4082 against B2's 0.4128, so that handicap explains only about 1% of the gap; the quantile model stays about 35% below either *[framing rule of 2026-09-21: read against B3a, about 13% lower]*.
  6. **Diagnostics.** Early stopping hit the 1,000-round cap in 5 of 12 final cells (raw was 9 to 12 of 12). The share of rows with crossing quantiles before sorting was 0.000 in every cell, so sorting never changed a forecast.
  7. **Caveats that limit what these numbers say.** (a) The advantage over B1 and B2 depends heavily on the design's sigma: a pooled RMSE of normalised residuals is inflated by stock-out-like zero runs and by heavy tails, which over-covers the 0.80 and 0.90 quantiles. A robust sigma (for example MAD-based) might narrow the gap; it was **not pre-registered and not tested**. (b) The four folds are used both to select and to report, so fold scores are mildly optimistic, and they precede the test window's regime (section 5). (c) The floor is at the top edge of its range for the second time. (d) The low-velocity segment, where no learned point model beat MA-28 in Phase 6, is also where the quantile model's advantage is smallest. (e) Nothing here is a claim about inventory or cost; that is Phase 10.
- 2026-09-21 **Phase 7 report-only sensitivities** (all at the adopted variant, tuning folds only; they change no decision). Mean scaled pinball over the 12 cells, base 0.2639:
  1. **Round cap and the normalisation decision.** The raw target refit at learning rate 0.1 (cap 1,000) scores **0.2721 mean and 0.1924 median**, against 0.2839 and 0.2049 at 0.05: a good part of the pre-registered raw deficit was under-convergence. **Normalised still wins, by 3.0% on the mean and 3.3% on the median**, still above the 1% bar, so the decision stands; but **the honest size of the normalisation advantage is about 3%, not the 7% of the pre-registered comparison** (about 41% of the mean gap remains). The normalised model itself is insensitive to convergence: 0.2643 at learning rate 0.1 (+0.13%).
  2. **Ids:** all four ids 0.2636 and all but `item_id` 0.2636 (both -0.11%): ids add nothing, as in Phase 6.
  3. **Year-ago `sum_364_P` removed from the normalised set:** mean 0.2636 (-0.12%) but median 0.1885 (+1.3%) and F2 0.2340 (+1.1%): mixed, so no case for changing the set (it stays, per the rule that follows the variant).
  4. **Future-price columns removed:** 0.2644 (+0.17%): the known-prices assumption is worth about 0.2% for the quantile model, negligible.
  **Phase 7 is complete** once its test record is logged and the phase declared done (section 15). Open items carried to later phases: the top-edge floor (1/7), the robust-sigma question for the point policy, and the uneven advantage across velocity segments.
- 2026-09-21 **Post-hoc benchmark B3, cells won and bootstrap, README statement (defined after seeing the Phase 7 results; committed before any simulation is run and before B3 is computed).**
  1. **B3: point forecast plus empirical residual quantiles from the calibration window. Post-hoc: it was added after the Phase 7 results were known, is not part of any pre-registered comparison, and can change no decision.** Purpose: the pre-specified point policy (section 8) uses a normal quantile with a pooled RMSE sigma, which over-covers the middle quantiles (Phase 7 entry); B3 removes the normal-sigma assumption so that it can be seen how much of the quantile model's advantage is that assumption and how much is the model. Definition, per model version: scores `s = (y - yhat) / scale` on the version's calibration Sunday origins (positive when the forecast was too low, `scale = max(mean_28, floor) * P`, frozen floor 1/7), pooled by velocity segment, `offset_seg,alpha` = the k-th smallest with `k = ceil((n + 1) * alpha)` (the conformal rule of the Phase 7 pre-registration, with the same too-few-scores guard), then `q_alpha = max(0, yhat + offset_seg,alpha * scale)`, sorted across alphas, for all six alphas. It is applied to three point forecasts: **B3a** the shared-set XGBoost mean (the point policy's model), **B3b** the frozen Phase 6 raw XGBoost (all Phase 6 features), **B3c** MA-28. Evaluated exactly as in Phase 7 (scaled pinball mean and median over series, coverage of `ceil(q)`, F2 separately, by segment and horizon).
  2. **Phase 10 use.** The headline comparison stays the pre-specified one (quantile policy vs point policy of section 8). **B3a will additionally be simulated as a separately labelled post-hoc policy variant, reported next to the pre-specified point policy and never replacing it.**
  3. **Cells won and item-cluster bootstrap (report-only).** For the quantile model against each of B1, B2, B2', B3a, B3b, B3c: the number of the 12 fold x horizon cells in which its mean scaled pinball is lower, and a 95% percentile interval for the difference in mean scaled pinball (quantile model minus benchmark; negative is better) from 10,000 resamples of the 100 items with each item's three store-series resampled together, seed 0, for all cells, F2 only, each velocity segment and each horizon. The interval reflects item sampling only, not period-to-period variability (one set of folds). The per-series values are recomputed by refitting the frozen configs and must reproduce the cached Phase 7 cell values to 1e-9.
  4. **README.** The README states that the quantile model's advantage over the point policy **concentrates in high-velocity series** (Phase 7: ratio 0.38 high, 0.76 mid, 0.91 low), and every number in it comes from a logged run (section 1).
- 2026-09-21 **Phase 8 pre-registration (explanations), committed before any Phase 8 code.** Rules set by the user, with my design choices marked (chosen).
  1. **Scope.** SHAP is built and tested on the tuning folds only. Explanations of test-window origins exist only in the final frozen run, and each is logged in the touch log (section 14) before it is shown. Code guard: origins whose target window passes the tuning cutoff are refused unless the caller passes an explicit test-window flag.
  2. **What is explained.** The **P50** (median) and the **policy service-level quantile** (one of 0.80, 0.90, 0.95, 0.99, chosen by the caller), of the final quantile model (normalised target, floor 1/7, depth 6 / `min_child_weight` 30, base feature set).
  3. **Method.** Exact per-quantile TreeSHAP contributions via XGBoost `pred_contribs` on the multi-quantile booster (the `shap` package's TreeExplainer is awkward for multi-quantile boosters, so it is not used or added as a dependency). Contributions are in the model's normalised ("ratio") space and **converted to units by multiplying by the row's scale** (`max(mean_28, floor) * P`); the bias term converts the same way and is reported as the baseline. Additivity holds for the raw model output of each quantile; clipping at zero and sorting are applied afterwards, and the explanation says so when the final value differs (sorting never changed a forecast in Phase 7).
  4. **Themes (chosen).** Correlated features are grouped for sentences and every input belongs to exactly one theme: *recent sales level* (lags, means, stds, `sum_last_P`), *zero-sales stretches* (`zero_frac_*`, `days_since_sale`, `zero_run_91`), *same period last year* (`sum_364_P`), *price* (`price`, `price_rel_now`, `price_mean_rel_p`, `price_min_rel_p`), *calendar* (SNAP, weekend, closed-day counts, `origin_dow`), *events* (event counts by type), *series age* (`age_days`). Theme contributions are sums of feature contributions, so they stay additive.
  5. **Sentences (chosen).** The top three themes by absolute contribution in units, ignoring those under 0.05 units; the only verbs are **"raises" for a positive and "lowers" for a negative contribution**, and a test asserts the verb always agrees with the sign. The baseline and the forecast are stated in units. Price wording is **"price is X% below usual"** (or "above usual", or "about usual" under 1%), never "promotion". Banned words: promotion, promo, discount, on sale, because, caused (associations, not causes; limitation 3 of section 2).
  6. **Only Sunday review origins are explained** (the weekly review dates of section 5); other weekdays are refused with an error.
  7. **Tests required before Phase 8 is done** (`docs/testing.md#shap`): additivity per quantile in ratio and in unit space; sign-to-word agreement; the unit conversion by hand and end to end; the wording rules; every input in exactly one theme and theme sums equal the total; Sunday-only and tuning-only guards. **The SHAP placeholder `tests/test_shap.py` is retagged from Phase 9 to Phase 8.** No reason to defer: it was tagged 9 in my earlier plan only because conformal held Phase 8, and conformal moved into Phase 7. `ml/record_tests.py 8` must record a passing run before the phase is declared done.
- 2026-09-21 **Correction: the Phase 7 crossing diagnostic was invalid, and a defect in XGBoost 3.4.1 (found while starting Phase 8).**
  1. **What was wrong.** The Phase 7 report and log said the share of rows with crossing quantiles before sorting was 0.000 and that "sorting never changed a forecast". That diagnostic measured XGBoost's joint `predict` output, which **returns the per-target tree sums already sorted** (checked: `predict` equals the sorted tree sums to 5.6e-6 on a toy model, and by hand-reconstructing predictions from the tree JSON). It could not see crossing. **The claim is withdrawn.**
  2. **The real rate,** measured on the unsorted per-target tree sums of the refitted frozen models (`ml/series_pinball.py`), share of rows whose quantile order the sort changed, per fold x horizon cell: **minimum 0.9%, median 2.6%, maximum 4.7%, mean 2.4%.** The reported quantiles are the monotone rearrangement, which is what the design specifies, so **every Phase 7 forecast and number is unchanged** (all cached cell values were reproduced to 1e-9); only the diagnostic statement was wrong.
  3. **Correction of the Phase 8 pre-registration, item 3.** It said the `shap` package's TreeExplainer is awkward for multi-quantile boosters and was therefore not used; that was **asserted without being tested**. Tested now: `shap` 0.52.0 returns exactly XGBoost's `pred_contribs` values and inherits the same additivity failure (on a toy multi-quantile model the contributions miss `predict` by up to 0.196 on 4 of 6 quantiles, because they explain the unsorted tree sums while `predict` returns them sorted); single-quantile boosters reconcile to 1e-6. **Method actually used:** one single-target booster per quantile is sliced from the frozen model's JSON (only that target's trees and base score), whose prediction is that target's tree sum and whose `pred_contribs` are exact; for each reported (sorted) quantile the contributions come from the target that supplied that rank's tree sum, so additivity holds on crossing rows too. Verified by tests (sorted sliced predictions equal the joint prediction; additivity per quantile to float32 precision; a fixture with crossing rows; a test that documents the XGBoost defect and will fail if an upgrade fixes it). `shap` is not a dependency. The slicing depends on XGBoost 3.4.1's JSON layout (pinned).
- 2026-09-21 **B3, cells won and the item bootstrap: results** (report-only; `docs/results/phase7_bootstrap.md`; tuning folds only, touch log unchanged; the per-series values reproduce the cached Phase 7 cell values to 1e-9). Mean scaled pinball over the 12 cells: quantile model **0.2639**; pre-specified point policy B2 0.4128; B1 0.4230; B2' 0.4082; **post-hoc B3a 0.3025** (median over series 0.2192), B3b 0.3069, B3c 0.3128. B3 coverage of `ceil(q)` is `consistent` in all 12 cells at all four levels (`cov_hi` / `cov_lo` 0.854 / 0.796 at 0.80, 0.928 / 0.899 at 0.90, 0.965 / 0.951 at 0.95, 0.993 / 0.991 at 0.99), like the quantile model.
  1. **About three quarters of the pre-specified advantage is the normal-sigma assumption.** Replacing "normal quantile around the mean with a pooled RMSE sigma" by "empirical residual quantiles from the calibration window" takes the XGBoost point policy from 0.4128 to 0.3025, which removes 74% of the 0.149 gap to the quantile model. **What remains is a smaller advantage: -0.0386 [-0.0493, -0.0312] against B3a (12.8% of B3a's 0.3025).** The headline comparison stays the pre-specified one (section 8), and this reading qualifies it.
  2. **Cells won and intervals.** The quantile model wins **12 of 12 cells against every one of the six benchmarks** (F2: 3 of 3), and every 95% item-bootstrap interval excludes zero, in every group (all cells, F2, each horizon, each velocity segment). Pooled difference in mean scaled pinball (negative is better): vs B2 -0.1489 [-0.1805, -0.1201]; vs B1 -0.1590 [-0.1925, -0.1284]; vs B2' -0.1443 [-0.1747, -0.1164]; vs B3a -0.0386 [-0.0493, -0.0312]; vs B3b -0.0430 [-0.0602, -0.0327]; vs B3c -0.0488 [-0.0550, -0.0432]. F2 alone: vs B2 -0.1544 [-0.1937, -0.1174], vs B3a -0.0289 [-0.0347, -0.0238]. The intervals reflect item sampling only, not period-to-period variability.
  3. **Where the advantage lives depends on the comparator.** Against the pre-specified point policy B2 it **concentrates in high-velocity series**: high -0.3401 [-0.3906, -0.2928], mid -0.0706, low -0.0356 [-0.0443, -0.0272]. Against B3a it is **small and roughly even**: high -0.0461, mid -0.0354, low -0.0342. So the concentration in high-velocity series is chiefly a property of the normal-sigma policy (its pooled RMSE sigma is inflated by heavy-tailed, stock-out-like residuals, which matter most for fast movers), not evidence that the quantile model is uniquely better for them. The README says so.
  4. **Phase 10.** As committed above, B3a is simulated as a labelled post-hoc variant next to the pre-specified point policy.
- 2026-09-21 **Amendment to the Phase 8 themes, after the first real-data run (tuning fold only).** The pre-registered *price* theme mixed the absolute `price` with the relative-to-usual features, and the sentence "the price (price is about usual) raises it by 1.1 units" then attributed a contribution to a price that the same sentence calls normal (the absolute price is an item-type proxy). **The theme is split:** `price level` (the absolute `price`, described as "the item's price level", never with "% below usual") and `price vs usual` (`price_rel_now`, `price_mean_rel_p`, `price_min_rel_p`), whose "price is X% below usual" wording now uses whichever of the current price, the planned window mean and the planned window minimum deviates most. Subjects were also made singular ("the calendar", "the event calendar", "the same period last year") so that "raises" and "lowers" agree. Tests were added for both.
- 2026-09-21 **Phase 8 results: explanations on a tuning fold** (`docs/results/phase8_explanations_tuning.md`, run at code commit `f9ede98` and reproduced identically on a second run; fold F2, P = 14, service-level quantile 0.90; **tuning fold only, no test-window origin was explained, touch log unchanged**).
  1. **Exactness over every explained row** (3,600 Sunday review origins x P50 and P90 = 7,200 values): baseline + sum of contributions equals the reported quantile before clipping at 0 to a maximum of 4.4e-4 units (5.0e-6 relative, float32 precision). On **89 of 3,600 rows (2.47%) the explained quantile came from another target's trees** because XGBoost's internal sort had changed the order (the fit's unsorted crossing share is 2.67%); the contributions add up on those rows too, which the naive joint `pred_contribs` would not. 75 rows have a raw quantile below 0 and are described as "floored at 0".
  2. **What the model uses** (share of the mean absolute contribution by theme, all velocity segments): the **P50** draws on the recent sales level 25.0%, the days-without-sales pattern 25.2%, the same period last year 17.9%, series age 16.0%, the calendar 6.6%, the item's price level 5.3%, **price vs usual 2.7%**, events 1.1%. The **P90** leans on the days-without-sales pattern (52.7%; 40.7% low, 54.6% high velocity) and last year's period (24.2%), with the recent sales level 10.7%, series age 5.0%, price level 4.0%, calendar 2.0%, **price vs usual 1.1%**, events 0.3%. The small price-vs-usual share agrees with the report-only future-price ablations (about 0.2% of scaled pinball).
  3. **Caveats.** These are associations in the model, not causal effects, and correlated inputs (the recent sales level and the zero-run features, for example) share credit in ways the theme grouping only partly resolves. That the upper quantile is driven mostly by the days-without-sales pattern fits the stock-out-like zero runs measured in Phase 3 but does not identify their cause. "Series age" carries 16% of the P50 attribution and its meaning is unclear (it may stand for a launch cohort or life-cycle effect); it is reported as found and not interpreted. One fold and horizon were explained; nothing here is a claim about other folds or the test window.
  4. **Not done, by design.** No test-window explanation exists. They are produced only in the final frozen run, each logged in section 14 before being shown.
- 2026-09-21 **Headline framing rule, and three changes requested after Phase 8** (committed before any code or run that depends on them).
  1. **Framing rule.** The quantile model's headline is always stated as: **"vs the post-hoc B3a benchmark (point forecast + empirical residual quantiles): about 13% lower scaled pinball on the tuning folds (0.2639 vs 0.3025, 12 of 12 cells); the normal sigma was most of the gap to the textbook policy (0.4128)."** The 36% figure against the pre-specified normal-sigma policy is **never quoted alone**. This applies to the README, the dashboard, every results file and any write-up.
  2. **Crossing.** The correction of 2026-09-21 stands. The **average crossing rate of 2.4%** (range 0.9% to 4.7% by cell) is reported in the model-performance table of the README and in the Phase 7 results file.
  3. **"Series age" is not interpreted in planner sentences.** Its contribution (16% of the P50 attribution in Phase 8) has no established meaning. The theme `series age` is replaced by the neutral theme **`other factors`** (it holds `age_days`), narrated only as "the remaining-factors group", and shown in global charts and tables as "series age (not interpreted)". **Post-hoc, report-only ablation, defined after seeing that share:** the final quantile model (normalised target, floor 1/7, depth 6 / `min_child_weight` 30, base feature set) is refit without `age_days` on the same 12 cells and reported next to the base (mean and median scaled pinball, F2, coverage status). It is labelled post-hoc, decides nothing, and would need its own pre-registered rule before any feature set changed.
  4. **README.** One line each on (a) the multi-quantile contribution defect in XGBoost 3.4.1 and the sliced-booster workaround, and (b) the 2.5% of explained rows (89 of 3,600 in the explained fold) whose quantile is explained through another quantile's trees.
- 2026-09-21 **Post-hoc, report-only: drop-`age_days` ablation (requested in the framing-rule entry above; the run is now in the cache).** Same quantile model and hyper-parameters, `age_days` removed from the shared feature set, tuning folds only: mean scaled pinball **0.2594 vs 0.2639 for the base set (-1.70%)**, median over series 0.1851 vs 0.1861, F2 0.2297 vs 0.2315, WAPE of the median 0.3530 vs 0.3579. This ablation was not pre-registered and was chosen after Phase 8 showed series age carrying 16% of the P50 attribution, so it is **post-hoc**. It is **report-only: the feature set, the frozen model and every number elsewhere stay as they are** (the 1.70% is above the 0.5% bar used for the pre-registered `ev`/`xs` additions, but that bar governs additions, and re-opening a frozen model after seeing the explanations would be a second use of the tuning folds for selection). Reading: the model is not helped by series age; it is a tuning-fold observation, not a validated improvement. If the feature is ever dropped it is a new pre-registered decision with its own evidence, not this entry.
- 2026-09-21 **Phase 9 pre-registration: planner-facing quantities (stockout-risk label, reorder point and safety stock, constant-CV check).** Tuning folds only; no simulation (that is Phase 10). Grids are those of section 10: service level alpha in {0.80, 0.90, 0.95, 0.99}, lead time L in {3, 7}, review period R = 7, so protection interval P = R + L in {10, 14} (P = 7 is display only); no interpolation between grids. Cut points and thresholds below are **fixed now and not tuned**.
  1. **Stockout-risk label.** For a series at a review date, let `IP` be its inventory position (on-hand plus on-order, integer units) and `Q_a = ceil(q_a)` the integer quantiles of protection-interval demand from the quantile model for the chosen P. **HIGH: `IP < Q50`. MEDIUM: `Q50 <= IP < Q90`. LOW: `IP >= Q90`.** A separate boolean **overstock flag** is raised when **`IP > Q99`**. Cut points are the grid quantiles P50, P90, P99. Caveats, stated wherever the label is shown: (a) demand is integer and the quantiles are rounded up, so `IP = Q_a` means the model puts at least `a` of its probability at or below `IP`; the chance of a stockout can be well below `1 - a` because of the probability mass exactly at `Q_a` (ties, large for slow movers); (b) when quantiles coincide (for example `Q50 = Q90 = 0`) the MEDIUM band is empty: `IP < Q` is HIGH, `IP >= Q` is LOW, and if `Q99 = Q90` the overstock flag fires at `IP > Q90`; for a series whose `Q99` is 0 it fires at one unit; (c) **the label is not a calibrated probability of stockout**: it compares stock with the model's protection-interval demand distribution and ignores censoring of the history, model error, and orders arriving inside the interval. **Report-only validity check on the tuning folds:** for reference positions `IP = ceil(c * mu28 * P)` with c in {1, 1.5, 2}, the share of origins per label and the realised frequency of `demand > IP` within each label; the label definition is not adjusted to it.
  2. **Reorder point and safety stock: classic vs quantile.** In this periodic-review system the reorder point is the order-up-to level, `ROP = S`, and the order quantity is `max(0, ROP - IP)`. Safety stock is `SS = ROP - yhat`, where `yhat` is the point forecast of protection demand from the XGBoost mean model (adopted variant, shared feature set) used for **both** methods so they are comparable; `SS` is shown floored at 0 and the raw value is kept. **Classic:** `ROP_c = ceil(max(0, yhat + z_alpha * sigma_P))`, `sigma_P = sigma_seg * scale`, `sigma_seg` the pooled RMSE of scale-normalised residuals over the version's calibration Sunday origins (per velocity segment), `z_alpha` the normal quantile. Assumptions: protection-interval demand is Normal around `yhat`; the forecast is unbiased; the residual standard deviation is proportional to the row scale within a segment (constant coefficient of variation); the error distribution in use matches the calibration window; the lead time is fixed. **Quantile:** `ROP_q = ceil(q_alpha)` from the sorted quantile model. Assumptions: no distributional form; the model is calibrated at `alpha` (tuning-fold coverage was consistent in Phase 7, not guaranteed elsewhere); stationarity between fit and use. Both use the same `ceil` rule and both are non-decreasing in `alpha`. A **post-hoc, labelled** third form, `ROP_B3 = ceil(max(0, yhat + scale * offset_seg,alpha))` with empirical residual offsets from the same calibration window, may be shown next to them and never replaces either.
  3. **Constant-CV check with a fallback.** On the calibration windows of the tuning-fold model versions (never the evaluation origins), for each velocity segment and horizon: order the series-origins by `scale`, split into 4 equal-count bins, and compute the RMSE of the scale-normalised residuals per bin. **Constant CV is accepted if the largest bin RMSE is at most 1.5 times the smallest** (threshold chosen now). Otherwise the **fallback** normalises residuals by `sqrt(scale)` (Poisson-like), `sigma_P = sigma'_seg * sqrt(scale)`, and the same check is applied to those residuals; if the fallback passes, the classic policy uses it for that segment and horizon and it is reported as a labelled variant `B2-sqrt`; if both fail, the original form is kept and the failure is reported. The Phase 7 B2 numbers stay as pre-specified and are not recomputed.
  4. **Phase 9 tests (fast, hand-computed):** classic reorder point by hand (yhat 10, sigma_seg 0.5, scale 4, alpha 0.9: `10 + 1.2815515655 * 2 = 12.563`, so `ROP = 13`, `SS = 3`); quantile reorder point by hand (`12.01 -> 13`, an exact `12.0 -> 12`, `0 -> 0`); monotonicity (both non-decreasing in alpha; classic non-decreasing in yhat, sigma and scale; label risk never rises as `IP` rises; the overstock flag is monotone in `IP`); integer `ceil` (integer outputs, never below the unrounded value); label boundaries by hand (`Q50 = 5, Q90 = 10, Q99 = 16`: `IP` 4 is HIGH, 5 and 9 MEDIUM, 10 and 16 LOW, 17 LOW with the overstock flag) and the degenerate cases; unsorted quantiles rejected; the constant-CV check on synthetic data of known structure (constant CV passes; sigma proportional to sqrt(scale) fails it and passes the fallback; a hand-computed bin ratio); grids enforced. `ml/record_tests.py 9` and a Linux container check at the Phase 9 commit before the phase is declared done.
- 2026-09-21 **Phase 10 pre-run rules, committed before any simulator code.**
  1. **Development on tuning-fold origins only.** The simulator, the policy code, its tests, all debugging, the naive c-grid and every parameter are developed and frozen on the tuning folds (each fold's model version, its 12 Sunday review dates, a four-week warm-up before its first review). No test-window origin is simulated during development.
  2. **One primary test-window run.** When the code and every parameter are frozen (their commit and grids recorded in this file), **the touch-log entry (section 14) is written and committed first**, and then exactly one primary run is made over the test window: base case (L = 3, R = 7, costs of section 9), the four policies below over their full parameter grids, the four warm-up and 25 review dates. Its output is the headline. It is re-run only if it crashes before producing results (logged); any code change after results are seen needs a new touch-log entry and is labelled post-hoc. The sensitivities (L = 7, random lead time, no future prices, backorder, the hindsight zero-demand-cycle diagnostic) are separate runs, each with its own earlier touch-log entry, made after the primary and unable to change it.
  3. **Naive c-grid widening rule (applied on the tuning folds, then frozen).** Start from c in {1.00, 1.25, ..., 3.00}. Widen **upward** in steps of 0.5 until the naive rule's pooled fill rate on the tuning folds is at least the highest pooled fill rate any other policy reaches at alpha = 0.99 (cap c = 10). Widen **downward** in steps of 0.25 (floor 0.25) until it is at most the lowest pooled fill rate any other policy reaches at alpha = 0.80. The resulting grid is written into this file before the test-window run and never changed afterwards.
  4. **The four policies.** (1) **Naive:** `S = ceil(c * mu28 * P)`. (2) **Point policy:** `S = ceil(max(0, yhat + z_alpha * sigma_seg * scale))`, the pre-specified normal form (with the constant-CV fallback as a labelled variant if Phase 9 triggers it). (3) **Quantile policy (headline):** `S = ceil(q_alpha)`. (4) **Post-hoc variant:** `S = ceil(max(0, yhat + scale * offset_seg,alpha))` with empirical residual offsets from the calibration window, labelled post-hoc and never replacing (2). The conformal-calibrated quantile is not simulated (no benefit in Phase 7). Headline framing follows the framing rule above.
  5. **Item bootstrap.** Cluster bootstrap over the 100 items (each item's three store-series resampled together), 10,000 resamples, seed 0, for: average on-hand inventory at matched achieved fill rate (linear interpolation between grid points, no extrapolation), fill rate and cycle service level at each setting, by velocity segment and by sub-period (holiday peak 23 Nov to 3 Jan, rest 4 Jan to 22 May). The interval reflects item sampling only, not variation between periods (there is one test period).
  6. **Expected under-service in the rest period, recorded before any simulation.** Achieved service in the rest period (4 Jan to 22 May 2016) is **expected to fall short of target for both the point and the quantile policy**, and by more than in the holiday peak, because volume in the rest period runs about 1.2 times the summer calibration-window level (a post-hoc observation, Phase 3) and calibration windows lag the regime (section 5). Direction only; no magnitude is claimed. The naive rule has no target, so it has no such gap. If the opposite appears, it is reported as such.
- 2026-09-21 **Phase 9 results on the tuning folds** ([`docs/results/phase9_policy_quantities_tuning.md`](results/phase9_policy_quantities_tuning.md), run at code commit `3781317`; refits reproduced all 12 cached Phase 7 cell values). Report-only; definitions were fixed above and are not adjusted to these numbers.
  1. **Constant-CV check fails for most segments.** Bin ratios (max/min RMSE of error/scale over 4 scale bins; threshold 1.5), median over the 12 cells: low 2.09, mid 3.84, high 12.36. The `sqrt(scale)` fallback passes for **low in 12 of 12 cells** (median ratio 1.23, max 1.47), for **mid in 6 of 12** (median 1.55, max 2.14) and **for high in none** (median 2.97, max 4.19). By the pre-registered rule the classic policy therefore uses the labelled variant **`B2-sqrt` for low (all cells) and for the 6 mid cells where it passes**, and keeps the original constant-CV form, with the failure reported, for the other 6 mid cells and all 12 high cells. Reading: for high-velocity series the scale-normalised error shrinks a lot as scale grows, so a pooled constant-CV sigma is far too wide for large-scale rows. The Phase 7 B2 numbers stay as pre-specified.
  2. **Classic vs quantile reorder points (P in {10, 14}, pooled).** Mean safety stock (units) and coverage of actual protection-interval demand at 0.80 / 0.90 / 0.95 / 0.99: classic 28.2 / 42.7 / 54.7 / 77.1 at coverage 0.944 / 0.969 / 0.979 / 0.988; quantile 4.1 / 7.5 / 11.3 / 32.0 at 0.844 / 0.917 / 0.960 / 0.996; post-hoc empirical residual 5.3 / 10.2 / 16.7 / 76.8 at 0.848 / 0.925 / 0.963 / 0.992. The classic reorder point **over-protects at every level below 0.99** (coverage well above nominal), which is the same behaviour Phase 7 found for its quantiles, and its excess is concentrated in high-velocity series (mean safety stock at 0.90: high 112.5 classic vs 13.5 quantile; mid 11.4 vs 5.5; low 4.1 vs 3.5). The quantile reorder point is close to nominal at 0.80 to 0.95. At 0.99 all three over-cover and the quantile method's 0.996 shows the 0.99 level is hard to hit with integer quantiles and few tail observations. These are coverage and safety-stock facts on the tuning folds, not inventory cost or service results (that is Phase 10). The classic figures inherit the failed constant-CV check above for high-velocity series and should not be read as a general statement about textbook policies.
  3. **Label validity (reference positions `IP = ceil(c * mu28 * P)`, P in {10, 14}).** Realised frequency of demand above IP is ordered as intended, HIGH > MEDIUM > LOW, at every c: c = 1: 0.450 / 0.304 / 0.119 (share of origins 26% / 73% / 0.4%); c = 1.5: 0.309 / 0.185 / 0.049 (10% / 46% / 44%); c = 2: 0.260 / 0.172 / 0.021 (9% / 21% / 70%). **HIGH is not a "more likely than not" label**: at c = 1.5 and 2 fewer than a third of HIGH origins had demand above IP, although IP below the model's median would suggest about half. The mismatch arises where the model's median sits well above `c * mu28 * P`, and the model overshoots there. This confirms the pre-registered caveat that the label is **not a calibrated probability of stockout**; the README and any UI must call it a risk band. **The overstock flag never fired at these reference positions** (0 origins at c up to 2), so this check says nothing about it; it needs positions well above Q99 and is exercised by the hand-computed tests only.
- 2026-09-21 **Phase 9 follow-up: why realised shortfall among HIGH rows is below one half** ([`docs/results/phase9_label_investigation_tuning.md`](results/phase9_label_investigation_tuning.md), `ml/label_check.py`, report-only, tuning folds, label definition unchanged). For a calibrated integer model `IP < Q50` implies `P(demand > IP) >= 0.5`, so the low figure needed an explanation or a bug. **No boundary bug was found:** tests now cover both sides of a ceiled tie for Q50, Q90 and Q99 (integer quantile, just above, just below, arithmetic noise) and the near-zero median case; the model's median itself is fine (share of origins with demand at or above `Q50`: 0.49 high, 0.49 mid, 0.45 low velocity). The low figure is a **selection effect of the reference positions plus rounding, not a mis-set cut point**:
  1. **Near-zero medians rounded up.** HIGH rows with `0 < q50 <= 1` (so `Q50 = 1`, and IP = 0 is HIGH) are 2,009 of the pooled HIGH rows at every c (27% of HIGH rows at c = 1, 68% at 1.5, 80% at 2) and have realised shortfall **0.174**. The model's continuous median of, say, 0.2 is an integer median of 0 for a slow mover (83.5% of origins with `q50` in (0, 0.5] have zero demand; 48.6% for (0.5, 1]); `ceil` turns it into 1 by the pre-registered rule.
  2. **Outages.** At c = 1.5 and 2, 97% to 100% of HIGH rows are origins inside a zero run of 14 or more days in the last 91 (`mu28` near zero, so the reference position is 0 or 1 while the model expects the item to trade); their realised shortfall is 0.26 to 0.30 (0.37 at c = 1). Reading: stock-out, delisting or seasonal absence (limitation 2), and the model cannot know which.
  3. **Where the label does what it says.** For rows with `q50 > 2` shortfall is 0.54 to 0.68, and it rises with the gap `Q50 - IP` (gap 1: 0.37 / 0.23 / 0.20 at c = 1 / 1.5 / 2; gap 2-3: 0.57 to 0.60; gap 4-10: 0.65 to 0.77; gap > 10: 0.81 to 0.85).
  **Consequences, none of which changes a definition:** the README calls the label a **risk band** (never a probability) and says HIGH rows with a median below one unit or inside an outage are much less likely to run short than the label suggests. **Display rule for any UI or API (fixed now):** a HIGH row with `q50 <= 1` is shown as "HIGH (median below one unit)", and a row inside a zero run of 14 or more days carries "long zero run: sales history may be censored". The same `ceil` rounding applies to the quantile policy's `S = ceil(q_alpha)` for near-zero quantiles, for every policy alike (section 8), and Phase 10 does not adjust it.
- 2026-09-21 **Known limitation and future work, `age_days`.** The post-hoc drop-`age_days` ablation (0.2594 vs 0.2639, -1.70%) is listed in the README as a known limitation and as future work. The frozen model keeps `age_days` in all simulator runs. If it is ever dropped it is a new pre-registered decision with its own evidence.
- 2026-09-21 **Phase 10: predictions, definitions and report rules, committed before any simulator code or run.** Supplements the pre-run rules above (they stand; where this entry is narrower it says so).
  1. **Development origins: F2 and F4 only** (narrower than "the tuning folds" in rule 1 above). F2 is the holiday fold, F4 the summer fold. Each fold uses its own model version (cutoff = first origin, fit end = cutoff minus 84 days, calibration on the 12 weeks before the cutoff), a four-week warm-up on the four Sundays before its first origin with the same model (their targets are after the fit end, so the model has not seen them), and its 12 Sunday origins. F1 and F3 are not simulated. The naive c-grid rule (rule 3 above) is applied to F2 and F4 pooled.
  2. **Predictions, direction only, made before any simulation** (each is reported as confirmed or refuted whichever way it falls):
     - **(a) Rest-period under-service.** Restating rule 6 above with the Phase 9 evidence in view: for the **quantile policy**, achieved cycle service in the rest period (4 Jan to 22 May 2016) falls **below target at each alpha** and by more than in the holiday peak. For the **point policy** only the relative claim is kept (its gap, achieved minus target, is smaller in the rest period than in the holiday peak): Phase 9 showed its classic reorder points over-cover on the tuning folds (0.944 / 0.969 / 0.979 at nominal 0.80 / 0.90 / 0.95), so an absolute shortfall is not predicted for it below 0.99 and the earlier "both fall short" wording is withdrawn as unsupported. The naive rule has no target and no gap.
     - **(b) 0.99 is inefficient for the quantile policy.** Between alpha = 0.95 and 0.99 the quantile policy's average on-hand inventory rises by more per percentage point of fill rate gained than between 0.90 and 0.95 (pooled, base case), and its achieved cycle service at 0.99 is at or above 0.99 in the holiday peak. Basis: on the tuning folds the quantile reorder point's mean rose from 27.4 to 48.2 (+76%) between 0.95 and 0.99 for a coverage gain of 0.960 to 0.996.
     - **(c) A smaller edge over B3a than over the point policy.** The inventory reduction of the quantile policy at matched fill rate, averaged over its four alpha points, is smaller against the post-hoc B3a policy than against the point policy, pooled and in the high and mid velocity segments (not predicted for low, where Phase 9 safety stocks are close: 3.5 vs 3.96 vs 4.1 at 0.90). The interval against B3a may include zero in some segments. Basis: scaled pinball 13% lower than B3a against 36% lower than the point policy, and reorder points 23.6 vs 26.3 (B3a) vs 58.9 (classic) at 0.90.
  3. **Labels and order in every report, file and figure.** The point policy is always labelled **B2 (point policy, normal sigma, constant CV, pre-specified)**, and where the Phase 9 check selects the fallback for a segment and model version the row is labelled **B2-sqrt**; the report states, per segment, in how many model versions each form was used. **B3a (point forecast + empirical residual quantiles, post-hoc) is the fair benchmark and is always presented first**, then B2 / B2-sqrt, then the naive rule. The Phase 7 framing rule applies to every comparison.
  4. **Simulator definitions (fixed now).** Timing as section 9: orders placed after close of review day `t`, arriving before opening of day `t + L + 1`; on-hand, in-flight orders and demand are integers; lost sales; demand is the M5 sales of the day, stores closed on a closed day sell 0. The **cycle of review `t`** is the seven days `t + L + 1 .. t + L + 7` (the arrival window of the order placed at `t`); its **cycle stockout** = any lost demand in those days; **cycle service** = share of metric cycles without a stockout; **fill rate** = units served / units demanded over the metric cycles' days, pooled; **average on-hand** = mean over series and metric-cycle days of end-of-day on-hand. Only the 25 test review dates (12 origins in a fold) count; warm-up cycles do not. Simulation starts at the first warm-up review with on-hand = S and nothing on order. Forecasts and the naive rule read the true M5 sales history, not simulated sales (section 9). Price for costs = item-store median `sell_price` over the evaluated days; unit cost 0.7 x price, holding 0.25/365 of unit cost per unit-day, stockout cost = `rho` x the holding cost of one unit over 7 days, `rho` in {4, 9, 19, 99}; cost-minimising setting per policy at each `rho`.
  5. **Matched-service statistic (fixed now).** For each of the quantile policy's four alpha points, the comparator's average on-hand inventory at the **same pooled fill rate**, linearly interpolated on the comparator's curve (sorted by fill rate, no extrapolation; a point outside the comparator's range is reported as not matched). Inventory reduction = `1 - inventory(quantile) / inventory(comparator at that fill rate)`. Reported per alpha and averaged over the alpha points that matched, with the item cluster bootstrap of rule 5 above (the same resampled items for every statistic). The four-point grid's coarseness is stated wherever the statistic is shown.
  6. **Model artifacts.** Fitted models used by the simulator (development folds and, later, the test-window versions v0 to v4) are saved with their `save()` methods to a git-ignored `models/` directory, each with a manifest entry (version, P, fit end, cutoff, columns, floor, code commit, SHA-256 of the file). The dashboard and API phase serves only these frozen files; nothing is refit at serve time.
- 2026-09-21 **Phase 10 development results, amendments and freeze** (development on F2 and F4 only; [`docs/results/phase10_simulator_dev_F2F4.md`](results/phase10_simulator_dev_F2F4.md), code commit `48e2152`). Tuning data only; touch log unchanged at this point.
  1. **What the development run showed** (F2 + F4 pooled, L = 3, P = 10, 300 series). Fill rate / cycle service / average on-hand (units per series-day): quantile policy 0.9195 / 0.886 / 9.7 (alpha 0.80), 0.9527 / 0.944 / 12.7 (0.90), 0.9716 / 0.970 / 16.3 (0.95), 0.9943 / 0.998 / 35.4 (0.99); B3a 0.9261 / 0.887 / 10.7, 0.9588 / 0.951 / 14.8, 0.9758 / 0.979 / 20.1, 0.9957 / 0.997 / 65.5; B2 / B2-sqrt 0.980 / 0.959 / 27.8, 0.9888 / 0.982 / 39.6, 0.9917 / 0.987 / 49.6, 0.9942 / 0.994 / 68.7. **Achieved cycle service was above target at every alpha for every method** (quantile: +0.086, +0.044, +0.020, +0.008), so the prediction of under-service (2a) is at risk on this evidence; it is left as committed and is judged on the test window only. The point-policy sigma form chosen by the Phase 9 check in the two model units: low sqrt-scale (2 of 2), mid one sqrt-scale and one constant-CV with the check failed, high constant-CV with the check failed (2 of 2). The quantile policy's marginal inventory per fill-rate point is 1.9 units between 0.90 and 0.95 and 8.4 between 0.95 and 0.99, consistent with prediction (2b); the comparator-anchored reduction is 3.9% against B3a and 30.1% against B2, consistent with (2c). These are development diagnostics, not tests of the predictions.
  2. **Amendment to the matched-service statistic (made after seeing development results, before any test-window run).** The pre-registered quantile-anchored statistic (rule 5 of the Phase 10 definitions) is **undefined against B2**: B2 over-protects, its fill rate at alpha 0.80 (0.980) already exceeds the quantile policy's at 0.95, so no quantile point lies inside B2's curve and, with no extrapolation, nothing is matched (0 of 4 settings). A **comparator-anchored statistic** is added and always reported next to it: at each of the comparator's own settings, the quantile policy's inventory interpolated at that fill rate, again with no extrapolation and the same item bootstrap. Because inventory is convex in fill rate, a straight line between two of four points lies above the true curve, so the quantile-anchored statistic tends to overstate the reduction and the comparator-anchored one to understate it: **they are reported as an upper and a lower bound**, not as two estimates (development: against B3a 22.2% and 3.9%). **Prediction (2c) is evaluated on the comparator-anchored statistic** (the only one defined for both comparators), pooled and in the high and mid segments, and the quantile-anchored version is shown for context. The four-point grid stays as pre-registered; no denser grid or extra models are added.
  3. **Naive c-grid.** The widening rule was applied on F2 + F4 pooled: the naive rule's fill rate saturates (0.9949 at c = 10) below the highest fill rate of another policy at alpha 0.99 (0.9957, B3a) because series whose 28-day mean is zero get S = 0 whatever c is, so the **cap c = 10 was reached**. **Frozen grid (23 points): c in {1.00, 1.25, ..., 3.00} then {3.50, 4.00, ..., 10.00}**; no downward widening was needed (c = 1.00 already gives a fill rate below the lowest of the others at alpha 0.80). The test-window run uses this grid as it is and does not widen it on test data.
  4. **Sub-period rule for the test window (fixed now).** A cycle belongs to the holiday peak if its first day, review date + L + 1, is on or before 2016-01-03, otherwise to the rest period. With review dates on Sundays this makes the cycles of the reviews of 22 Nov to 27 Dec (6 reviews) the peak (the 27 Dec cycle, 31 Dec to 6 Jan, straddles the boundary and counts as peak) and the 19 reviews from 3 Jan to 8 May the rest.
  5. **Cost sweep.** Development cost minima sit at the edge of the policies' own grids: alpha 0.80 for the target-driven policies at rho 4 and 9 (also B2 at rho 19), alpha 0.99 for the quantile policy at rho 99, and c = 1.00 for the naive rule at rho 4, where its total, 1,113, is below the quantile policy's 1,137. The report flags every minimum that sits at a grid edge; nothing is extrapolated.
  6. **Frozen for the primary run.** Code at commit `48e2152` and any later commit that changes only documents or this log; base case L = 3, R = 7, P = 10; the Phase 7 model (normalised target, floor 1/7, quantile XGBoost with max_depth 6 and min_child_weight 30, mean XGBoost with max_depth 3 and min_child_weight 30) refit by `ml/sim_rows.py test` for model versions v0 to v4 (cutoffs 2015-10-25, 2015-11-22, 2016-01-17, 2016-03-13, 2016-05-08; fit end cutoff minus 84 days; calibration on the 12 weeks before each cutoff); four warm-up reviews (25 Oct, 1, 8, 15 Nov 2015) and 25 counted reviews (22 Nov 2015 to 8 May 2016), the last cycle ending 18 May 2016; policies naive over the frozen grid, B2 / B2-sqrt, quantile and B3a over alpha in {0.80, 0.90, 0.95, 0.99}; 10,000 item-bootstrap resamples with seed 0; costs as section 9 with unit price the item-store median `sell_price` over the evaluated days and `rho` in {4, 9, 19, 99}; the predictions, definitions and report rules above. `ml/sim_run.py primary` refuses to start without a touch-log row for the primary run and refuses to overwrite an existing result file. The sensitivities (L = 7, random lead time, no future prices, backorder, the hindsight zero-demand-cycle diagnostic) are not built yet and each needs its own earlier touch-log entry.
- 2026-09-21 **Phase 10 primary test-window run: results** ([`docs/results/phase10_simulator_primary.md`](results/phase10_simulator_primary.md), made once at commit `1dec014` after the touch-log entry of the same commit; nothing was changed after seeing it). Base case L = 3, R = 7, 25 counted reviews, model versions v0 to v4, 100 items, 300 series. This is the only test-window result so far; the sensitivities are not run.
  1. **Curves** (fill rate / cycle service / average on-hand, units per series-day). Quantile policy: 0.9318 / 0.885 / 10.3 (alpha 0.80), 0.9611 / 0.941 / 13.5 (0.90), 0.9768 / 0.971 / 16.9 (0.95), 0.9958 / 0.997 / 37.7 (0.99). B3a: 0.9327 / 0.889 / 10.9, 0.9636 / 0.949 / 15.3, 0.9797 / 0.978 / 21.3, 0.9955 / 0.997 / 69.7. B2 / B2-sqrt: 0.9764 / 0.959 / 24.5, 0.9860 / 0.980 / 34.2, 0.9898 / 0.988 / 42.4, 0.9934 / 0.993 / 58.0. Naive: 0.8576 / 0.745 / 7.2 (c = 1.00), 0.9742 / 0.960 / 21.1 (2.00), 0.9885 / 0.986 / 36.9 (3.00).
  2. **Prediction (2a), rest-period under-service: NOT CONFIRMED (refuted).** Achieved cycle service was **above target at every alpha in the rest period for the quantile policy** (+0.086, +0.043, +0.022, +0.008 at 0.80 / 0.90 / 0.95 / 0.99) and for the point policy (+0.162, +0.081, +0.039, +0.005), and the rest-period gap was **not** smaller than the peak gap for the quantile policy (peak +0.082, +0.032, +0.017, +0.005), the opposite of the prediction; the point policy's rest gap was also larger than its peak gap at all four alphas. The premise (higher volume in the rest period than the calibration windows saw) did not translate into under-service. The peak was closer to target than the rest, by 0.4 to 1.1 percentage points at alpha up to 0.95 for both policies.
  3. **Prediction (2b), 0.99 inefficient for the quantile policy: CONFIRMED.** Marginal inventory per percentage point of fill rate 2.17 (0.90 to 0.95) against 10.93 (0.95 to 0.99); cycle service at 0.99 in the peak 0.9950, at or above 0.99.
  4. **Prediction (2c), smaller edge over B3a than over the point policy: NOT CONFIRMED by the rule as pre-registered, holds in part.** Comparator-anchored mean reduction: pooled 16.0% against B3a vs 29.5% against B2 / B2-sqrt (smaller, as predicted); high 20.0% vs 26.0% (smaller); **mid 17.7% vs 10.6% (larger, prediction fails)**; low 4.6% vs 7.6%. The rule required pooled, high and mid, so the verdict is not confirmed. Quantile-anchored: 12.7% against B3a and 31.8% against B2 (1 of 4 settings matched; 58% of resamples).
  5. **Headline against the fair benchmark, in the framing of this file.** Against **B3a**, for service targets 0.80 to 0.95 the quantile policy needs **about 5% to 16% less inventory at the same fill rate**, depending on the anchor: comparator-anchored 4.5%, 7.7% and 5.6% at B3a's alpha 0.80, 0.90 and 0.95; quantile-anchored 9.2% and 16.2% at its own alpha 0.90 and 0.95 (0.80 and 0.99 not matched). At the 0.99 end the difference is large, **46.3% (comparator-anchored)**: B3a needs 69.7 units per series-day for a fill rate of 0.9955 that the quantile policy reaches with 37.7 (0.9958). The pooled means over matched settings are 16.0% (comparator-anchored, four settings, item interval [0.7%, 24.9%]) and 12.7% (quantile-anchored, two settings, [9.5%, 24.4%]); **they are means over different matched sets and the first is dominated by the 0.99 point, so they are not ordered as the interpolation-bias argument of the amendment predicts and are not to be read as an upper and a lower bound on one quantity** (the bias argument holds point by point, not for means over different sets of points). The intervals reflect item sampling only, not variation between periods. Against **B2 / B2-sqrt**: 29.5% (comparator-anchored, four settings, [21.9%, 35.9%]). Against the **naive rule**: 45.3% (comparator-anchored, 21 of 23 settings, [12.6%, 52.4%]).
  6. **Segments and periods.** Against B3a (comparator-anchored): low 4.6% [-2.8%, 15.6%], mid 17.7% [1.1%, 27.2%], high 20.0% [-3.1%, 26.2%]; peak 4.8% [-7.2%, 20.5%], rest 20.6% [9.0%, 26.3%]. **The intervals for low and high velocity and for the peak include zero**: against the fair benchmark the edge is not established in every subgroup.
  7. **Point-policy sigma forms** over the five model versions: low sqrt-scale in 4 and constant CV with the check failed in 1; mid sqrt-scale in 1 and failed in 4; high failed in 5. B2 for high and most mid rests on a constant-CV form the Phase 9 check rejected, so its large deficits there partly measure that assumption and are not a general statement about textbook policies.
  8. **Costs** (sensitivity; minimum over each policy's own grid, `rho` 4 / 9 / 19 / 99): quantile 1,213 / 1,503 / 1,931 / 3,617; B3a 1,281 / 1,555 / 2,069 / 3,819; B2 2,293 / 2,413 / 2,652 / 4,475; naive 1,174 / 1,604 / 2,146 / 4,223. **The naive rule has the lowest cost at rho 4** (its minimum is at c = 1.00, the edge of its grid, so its true minimum may be lower). The quantile policy is lowest at rho 9, 19 and 99.
  9. **Report layout defect (cosmetic, not fixed in the result file).** The generated report prints the heading "2. By velocity segment" twice, with the two-anchor reading paragraph under the first; the numbers are unaffected. The generator is left as run; a layout fix would be a code change after the run and is deferred to the sensitivities' runs.

## 13. Left to settle in later phases

- Phase 4 is done: feature list, windows and the 182-day price-median window are in `docs/features.md`. Modelling choices in it (windows, caps, normalization later) are decided or changed on the tuning folds only.
- Phase 6: normalization scale floor; XGBoost hyperparameters (tuned on the rolling tuning folds).
- Phase 9: how the API exposes the "stockout risk" label (HIGH/MEDIUM/LOW) thresholds.
- Phase 3 is done (closed-day rule, terciles, subset; see the decision log). Open from it:
  - **Dead series at the cutoff (Phase 6):** 28 series had zero sales in the 28 days before the cutoff; the normalization scale floor must handle them.
- **Phase 7 settles** its items under the rules pre-registered in section 12 (Phase 7 pre-registration): the final normalisation decision on scaled pinball including the floor re-test, the ablations for the quantile model, coverage, and conformal calibration. Carried over from Phase 6: low-velocity series, where no learned point model beat MA-28; and applying the recorded point-model ablation outcomes (ids removed; year-ago follows the chosen variant) once the variant is fixed.
- Phase 10: implement the warm-up (version-0 model, simulation from 2015-10-25) and the hindsight "exclude zero-demand cycles" diagnostic as decided in section 12.

## 14. Test-window touch log

Every time anything (a metric, a plot, a table, a model comparison, a threshold, a hyperparameter) is computed from data whose demand day falls in the test window (2015-11-23 to 2016-05-22), it is recorded here with date, what was computed, and whether it informed any decision. Tuning-stage code calls `folds.assert_tuning_only`. Model versions that retrain during the test window (section 5) use only data before their own cutoffs and are not touches.

| Date | Phase | What was computed on test-window data | Informed a decision? |
|---|---|---|---|
| 2026-09-21 | 10 | **Phase 10 primary run**, written before it starts. (1) `ml/sim_rows.py test`: forecast tables and calibration rows for model versions v0 to v4, i.e. features and predictions at the 29 review dates 2015-10-25 to 2016-05-08 (their features use sales up to each review date) and the calibration windows before each cutoff (targets observed by the cutoff). (2) `ml/sim_run.py primary`: one replay of the naive (frozen grid), B2 / B2-sqrt, quantile and B3a policies against the M5 sales of 2015-10-26 to 2016-05-18, base case L = 3, plus its item bootstrap, sub-period, segment and cost tables and the mechanical evaluation of the three predictions; output `docs/results/phase10_simulator_primary.md`. Code frozen at `48e2152`, freeze recorded at `f52daea`. | No: it is the headline result. It cannot change code, grids or parameters; any change after seeing it needs a new entry here and is labelled post-hoc. | |

The Phase 3 EDA looked at test-window *volume* before this log existed; that is disclosed in section 12 (decision 5 after Phase 3) and is not repeated here.

## 15. Testing and definition of done

From Phase 5 on, a phase is complete only when its tests are written and pass: the fast suite (synthetic fixtures, no data, run by CI on every push) and, where marked, the real-data checks (`pytest -m realdata`). Tests assert definitions, invariants, hand-computed values and synthetic ground truth, never unmeasured accuracy. What each component must be tested for, and what is still pending, is in `docs/testing.md`; a skipped placeholder there blocks its phase. **Phase gate:** phases are declared done in `docs/phase_status.json`; `tests/test_phase_gate.py` fails if a placeholder tagged `PENDING-PHASE: N` remains for a done phase N, if a model a done phase must deliver is not registered, or if `docs/results/test_log.md` has no passing fast + real-data record for it. The record is written by `uv run python ml/record_tests.py N` at the phase's commit.
