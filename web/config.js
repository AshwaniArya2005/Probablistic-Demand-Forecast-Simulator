// Set API_BASE to the deployed Express service (no trailing slash). Empty means: show the recorded snapshot only.
window.DEMO_CONFIG = { API_BASE: 'https://demand-forecast-demo-api.onrender.com', TIMEOUT_MS: 8000, WAKING_AFTER_MS: 3000 };
